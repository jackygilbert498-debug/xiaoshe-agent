# 本次交接验收

更新时间：2026-08-30（Asia/Shanghai）

## 结论

本目录是可继续开发的完整 XS 工程，不是只有说明文档的快照。正式交接包会同时携带：

- XS 产品层源码与当前工作树；
- `runtime/DSH` 完整源码和 Git 历史；
- `runtime/xiaoshe-legacy` 兼容层及唯一正式品牌资产；
- Phase 7 可重定位离线工件；
- 接收、安装、验证、启动和重新打包工具；
- 三个仓库的 HEAD、工作树指纹与逐文件 SHA-256 完整性清单。

凭据、API Key、个人会话、本机状态、依赖目录、构建产物和缓存不会进入交接包。接收设备需要重新配置模型凭据和 macOS 权限。

## 本机已通过的验证

2026-08-30 壳对比五项目三轮审计：对除已处理 `codex-main`/DSH 外的 `cc-haha`、`CodeWhale`、`hermes-agent-main`、`kimi-code`、`openclaw-main` 完成三轮只读扫描，只把插件人类身份与来源核验这一项真实缺口接入既有 `@xiaoshe/plugin-governance`。公共 API/UI 不泄漏本地绝对路径、URL 凭据或双向控制字符；摘要不冒充签名，来源明确为未签名/未核验，Host/无独立 OS 沙箱边界可见。plugin-governance `39/39`、adapted `69/69`、Product Bundle `7/7`；新鲜根 `pnpm.cmd check` 为 34 files / 113 PASS + 3 platform skip，Python `15/15`，真实 Profile、浏览器、12 包搬移、typecheck/build 通过。Windows 临时 Profile 的自有孤儿锁也已用“退出子进程 PID 必须与锁所有者一致”的边界修复并纳入回归。证据见 `docs/evidence/shell-comparison-plugin-provenance/acceptance.md`；macOS 最终实机仍为 `PENDING`。

2026-08-30 助手正文阅读密度收口：真实浏览器确认共享 Markdown 根节点曾把助手正文覆盖为 `16px / 28px`；现在只在 adapted 壳边界改为 `14px / 24.08px`，展示标题仍为 `22px / 32px`，用户消息仍为 `13.5px / 24.3px`，共享 DSH primitive、代码块、左右栏与输入器未改。专项 RED→GREEN 后 adapted `68/68`、typecheck/build 通过；根 `34 files / 113 PASS + 3 platform skip`、Python `15/15`、四套真实 Profile 与生产构建通过。正式 3080 的 390、760、1024、1280、1440 五档均无横向溢出，控制台 `0 error / 0 warning`。macOS 最终实机项继续为 `PENDING`。

2026-08-30 三轮复核后最终收口：补齐内容/推理边界、会话级草稿、历史/目录分页、共享弹层、记忆项目键、Product Health 插件边界、44px 手机触控区、插件语义分组和设置焦点恢复。消息定位点击与激活算法统一到 `34%` 阅读线，并用 `1px` 容差吸收浏览器布局取整；正式 `1280×900` 点击第 3 条后当前项为 3、目标与阅读线只差 `0.02px`。新鲜 adapted `67/67`、DSH 通用设置 `18/18`、根 `34 files / 113 PASS + 3 platform skip`、Python `15/15`，四套真实 Profile、typecheck/build 全部通过。正式 3080 五档宽度无页面级横向溢出，新页面控制台 `0 error / 0 warning`。macOS 最终实机、权限、多显示器和真实桌面动作保持 `PENDING`。

2026-08-29 用户追加视觉收口：消息定位器已移除横竖相交的连续导轨，改为独立、交错的玉色鳞片；正式对话右下官方轮廓水印保持既有 CSS 放大尺寸，`1440×900` 仍为 `270×270`，只把底部间距由 `45px` 调到 `14.4px`；开始前“小蛇”旁线稿单独限制为 `min(280px,42%)`，再向右 `32–56px`、向下 `10–18px`。已命名、未命名和新会话标题最终全部统一为 `18–23px / 500` 的轻雾面元信息。专项 RED→GREEN 后正式 adapted `60/60`、typecheck/build 通过，根门为 `112 PASS + 3 platform skip`、Python `15/15`；正式 `3080` 的 `2269×1214`、`1440×900`、`1024×768`、`740×900`、`390×844` 五档无横向溢出，亮/暗控制台 `0 error / 0 warning`。macOS 实机验收继续由接收设备完成。

2026-08-29 三轮体验审计最终修复：运行中“停止 / 调整方向”、真实 `/compact`、插件目录友好命名与去重、中性服务商新增、同草稿记忆放大编辑、主题单一真源、长标题/历史错误/折叠推理、会话标题去重、轻量消息定位和回底、权限/思考强度向上弹层、键盘/读屏/触控与五档自适应均已落入正式壳。新鲜 adapted `58/58`、Runtime Contract `19/19`、Runtime Provider `19/19`、Task Timeline `3/3`、DSH 设置 `524/524` 及相关 typecheck/build 通过。正式 `3080` 覆盖 1440、1280、1024、760、390 五档，横向溢出和标题碰撞均为 0，控制台 `0 error / 0 warning`。macOS 实机、系统权限、多显示器和真实桌面动作仍明确为 `PENDING`。证据见 `docs/evidence/native-shell-legacy-adapted/2026-08-29-ux-repair-acceptance.md`。

2026-08-29 正式交互与插件精修补充：插件清单已从原始 `152` 个模块、`178` 个实例整理为 `37` 张无重名能力卡和六个真实分组，技术模块与实例仍可展开追溯；正式输入器已支持运行中停止生成和通过 `steer` 即时补充方向。消息定位器改为细线玉色节点，回底按钮降为低对比按需悬浮，官方线性蛇标在亮/暗主题下恢复可辨识度，非空对话右下恢复同源官方轮廓水印。正式 adapted `52/52`、Runtime Provider `18/18`、根门 `112` 项通过/`3` 项平台跳过、Python `15/15`，插件设置专项 `9/9`，两套插件构建与 DSH Profile 解析通过；正式 `3080` 插件页为 `0` 重名、`0` 溢出、`0` console error。证据见 `docs/evidence/native-shell-legacy-adapted/2026-08-29-interaction-plugin-visual-refinement.md`。

2026-08-29 Windows 接收补充：从 `C:\Users\example\Downloads\XS交接` 按来源清单增量接入更新 42、新增 51、删除 0；93/93 条同步路径哈希一致，42 个旧文件有可恢复副本。官方 CheckOnly 与正式接收安装均退出 0；正式 adapted 49、terminal-client 14、runtime-contract 18、runtime-dsh-provider 18 项通过，根门 112 项通过/3 项平台跳过，Python 15/15，typecheck/build、真实 Profile、Phase 7 和篡改回归均通过。正式 3080 浏览器 0 error / 0 warning，真实消息索引悬停与跳转通过；完整与自适应视觉矩阵均 PASS。跨平台验收脚本的 macOS 日志路径硬编码已改为 Windows/macOS/Linux 平台正确断言。证据见 `docs/evidence/cross-device-sync/2026-08-29-macos-to-windows-receipt.md`。

2026-08-28 换机前最终补充：长会话“我的消息索引”已按用户消息数量自适应，最高 `min(48vh, 420px)`，超出后内部滚动；悬停摘要脱离滚动裁剪区，点击仍回到原消息。隔离浏览器实测 3 条消息为 `52px` 且无滚动，40 条消息封顶 `420px` 且 `scrollHeight=718px`。根工程 `32 files / 108 tests PASS + 6 platform skip`、Python `15/15`、typecheck/build 均通过；Phase 7 为 10 包、254933 字节、8 端点 200、路径泄漏 0。正式 3080 Client 与本地构建哈希一致。换机预检还恢复了全部 macOS `.command` 入口执行位，并为打包暂存和真实解包新增双重执行位门禁，防止 Finder 双击再次报权限错误。证据见 `docs/evidence/native-shell-legacy-adapted/2026-08-28-message-index-acceptance.md`。

2026-08-28 macOS 后台补充：终端共享运行时与“工作现场侧舱”V1 本地工程主线已完成。全工作区 19/20 个可执行项目 typecheck/build 通过；正式 adapted 46、runtime-contract 17、runtime-dsh-provider 16、terminal-client 14 项测试通过；根 `pnpm run check` 为 32 files / 108 tests PASS + 6 platform skip，Python 15/15。真实 DeepSeek 返回精确验收文本并恢复 34 个持久事件。Phase 7 为 10 包、248976 字节、8 端点 200、路径泄漏 0，正式离线目录已重建。按用户要求未打开界面或执行屏幕、鼠标、键盘动作；浏览器手动交互和跨平台视觉复验仍明确保留。证据见 `docs/evidence/work-surface-dock-v1/acceptance.md`。

本次 Windows 最终复验运行时：Node.js `24.18.0`，pnpm `11.7.0`。历史 macOS 证据使用 Node.js `24.19.0`；二者均属于锁定的 Node 24 支持范围。

2026-08-27 Windows 接手机补充：接收安装已从入口退出 0；根 Vitest 111 项通过、1 项平台条件跳过，Python 15/15，18 个工作包的 build/test/typecheck 以及插件治理、V6、正式壳、Phase 7 真实 Profile 均通过。接收过程中修复了 pnpm Junction 导致的 Profile 递归备份失败，以及 adapted 来源测试未过滤 `.DS_Store`、未统一路径分隔符的问题；两项均有回归测试。

2026-08-27 正式界面补充：权限/推理上弹菜单、细线水印、可编辑记忆、插件贡献式设置、真实发送偏好、长会话导航、展示字体、手机设置布局和全页多比例自适应均已落入正式 adapted 包。最新根门禁仍为 111 项通过、1 项平台跳过，Python 15/15；正式 `3080` 页面在真实 Chromium 中为 0 error。专项验收覆盖 1180、2560、1920、1280、740、390、375 七档和亮/暗设置，完整交互回归覆盖 2269、1440、1024、740、390 五档。证据见 `docs/evidence/native-shell-legacy-adapted/2026-08-27-refinement-acceptance.md` 与 `adaptive-acceptance.json`。

2026-08-27 最终来源闭环：Downloads 来源 manifest 的 10,552 个文件在当前正式工作区中 0 缺失；10,523 个逐字节未变，29 个是同步后的前向修复、测试、说明或截图。`验证交接-Windows.ps1 -Full` 新鲜退出 0，`xiaoshe-doctor` 为 `ready=true / failed=0`。完整映射见 `docs/evidence/cross-device-sync/2026-08-27-final-handoff-audit.md`。

2026-08-27 macOS 接收补充：新输入清单相对本地上一版新增 116、修改 29、删除 0；145/145 条同步路径哈希一致。macOS `验证交接-macOS.command --full` 退出 0：18/19 个工作包全门禁、根 106 项通过/6 项平台跳过、Python 15/15 及真实 Profile 均通过；按用户要求未打开小蛇界面。详细记录见 `docs/evidence/cross-device-sync/2026-08-27-windows-to-macos-receipt.md`。

- 旧版适配界面：8 个测试文件、41 项测试全部通过，类型检查和生产构建通过；
- XS 根工程：34 个测试文件中 33 个通过、1 个仅因平台条件跳过；111 项通过、1 项平台条件跳过；
- Python 桌面桥：15 项测试全部通过；
- 工作区：18/19 个工作包进入执行范围，递归 build、test、typecheck 全部退出码 0；
- Phase 7 重定位验收：10 个离线包、232392 字节；源目录被隐藏后仍可启动；8 个真实 HTTP 端点均返回 200；源路径泄漏 0；目标机本地覆盖 10 项；源哨兵未变化；
- 交接完整性：打包前、临时副本、解包后三次执行文件哈希、Git 对象、HEAD 与工作树指纹校验。

## 明确没有冒充完成的事项

- macOS 最终实机、系统权限、多显示器、跨屏拒绝边界和真实桌面动作没有在 Windows 冒充通过；接收设备需运行 `pnpm run accept:macos-desktop` 与 `pnpm run accept:macos-multidisplay-safety` 并人工复验；
- “工作现场侧舱”V1 已有代码、接口、单元/集成门禁与离线工件，正式 adapted 页面和消息索引也已在浏览器复验；但当前持久会话没有结构化 surface 事件，所以本轮没有伪造侧舱打开交互。真实网页、文件、终端或桌面 surface 的打开、切换、调宽和恢复仍是 `release-held`。边界见 `下一步-工作现场侧舱.md`；
- 当前 Windows 本机浏览器已复验；Linux、不同 DPI、多显示器和另一台真实设备的权限/显示复验仍必须在对应接收设备执行；
- 本次交接未代替用户执行提交、合并、发布或线上部署。

## 接收设备最短路径

1. 校验交接包旁的 `.sha256` 文件；
2. 解压后先读 `交接工具/从这里开始.md`；
3. 运行对应平台的 `接收并安装-*`；
4. 运行对应平台的 `验证交接-*`；
5. 使用 `启动小蛇.command` 或对应平台入口启动；
6. 先确认当前界面、真实模型回合和审批动作，再继续新功能开发。

验收失败时不要覆盖本机原项目；保留终端完整输出并按 `交接工具/故障恢复.md` 处理。
