# XS 小蛇当前状态（2026-08-30）

## 一句话结论

当前工作位于 `codex/xiaoshe-native-shell`，基准提交仍为 `b10ef68d13203ede11eefcd3dabbe429c85d8b82`。Phase 4–8 产品能力、真实 Profile、跨设备工件和 macOS 真实桌面动作均保留。正式 `@xiaoshe/native-shell-legacy-adapted` 继续从老版母版的冻结副本删改适配，没有覆盖 `runtime/xiaoshe-legacy/ui`、原 `@xiaoshe/native-shell` 或 V1–V6。2026-08-29 已把另一台设备的终端共享运行时、工作现场侧舱 V1 和消息索引最新收口增量接入 Windows 正式工作区，并完成官方接收安装、正式 3080 浏览器与多尺寸验收；真实结构化 surface 驱动的侧舱打开交互、目标显示器、DPI 和另一台真实设备复验仍为 `release-held`。全部进度仍在未提交工作树中；这不是合并、提交或发布批准。

## 2026-08-30 壳对比五项目三轮审计与插件身份来源

- 已对 `cc-haha`、`CodeWhale`、`hermes-agent-main`、`kimi-code`、`openclaw-main` 完成架构、真实调用链和反向查漏三轮只读扫描；实施前后入口哈希、HEAD 与工作树计数一致。
- 唯一立即接入的真实缺口是插件的人类名称、用途、开发者/许可证、来源选择与保证等级。能力归属现有 `@xiaoshe/plugin-governance`，没有新建第二套 Loader、目录、事务账本或运行时。
- 本地绝对路径、URL 凭据/query/fragment、控制字符与双向覆盖字符不会进入公共 API/UI；字节摘要不冒充签名，所有来源明确显示为未签名/未核验，Host 进程与无独立 OS 沙箱边界可见。
- Windows 验收发现并修复了临时 Profile 被强制停止后可能残留的自有 `settings.yaml.lock`：只在子进程退出且锁 PID 完全匹配时清理，不修改 DSH 产品锁协议。
- 新鲜门禁：plugin-governance `39/39`、adapted `69/69`、Product Bundle `7/7`；根 `pnpm.cmd check` 为 34 files、113 PASS + 3 platform skip，Python `15/15`，真实 Profile、typecheck/build 通过；12 包搬移后 8 端点 200、0 路径泄漏。浏览器真实 DOM 与 console 验收通过。
- CodeWhale 的 side-Git 恢复、OpenClaw 的远程通道配对、Hermes 的无来源迁移器和 cc-haha 的重复质量分类器均因真源冲突或当前无消费者而暂缓，不伪造完成。完整凭证见 `docs/evidence/shell-comparison-plugin-provenance/acceptance.md`；macOS 实机保持 `PENDING`。

## 2026-08-30 codex-main 三轮参考审计与会话连续性插件

- 对只读参考源 `C:\Users\example\Desktop\壳对比\codex-main` 完成架构能力、真实调用链、反向查漏三轮扫描；实施前后六个入口哈希一致，没有修改或复制参考代码。
- 唯一确认值得接入且尚未交付的缺口是模型侧既往会话查询。Product Bundle 现在通过独立 Cordis row `xiaoshe-session-continuity` 装配 DSH 原生授权 Consumer；DSH Session Log 仍是唯一权威，`DSH_HOME/session-query.sqlite` 只在第一次搜索时建立可重建索引。
- 模型可按当前工作区安全使用 `session_search`、`session_event_search`、`session_trace`、`session_event_trace`、`session_event_read`；卸载 Product 后五工具与 row 消失、Provider 恢复关闭默认值、会话 sentinel 不变。
- 插件管理把两个内部标识折叠为“会话连续性”，不会暴露技术包名。正式离线目录已重建为 12 个独立工件，迁移证明为 294429 bytes、12 个本地 override、8 端点 `200`、0 路径泄漏。
- 新鲜门禁：Product Bundle `7/7`、adapted `68/68`、DSH 查询专项 `172/172`；根 `pnpm.cmd run check` 为 `34 files / 113 PASS + 3 platform skip`，Python Bridge `15/15`，真实 Profile、类型检查与生产构建通过。完整证据见 `docs/evidence/2026-08-30-codex-main-plugin-integration-acceptance.md`；macOS 实机保持 `PENDING`。

## 2026-08-30 助手正文阅读密度最终收口

- 根因是共享 Markdown 根节点把壳层 `13.5px` 正文覆盖为 `16px / 28px`；现在只在 adapted 壳边界将助手 Markdown 普通正文改为 `14px / 24.08px`。展示标题仍为 `22px / 32px`，用户消息仍为 `13.5px / 24.3px`，没有修改共享 DSH primitive、代码块、左右栏或输入器。
- 专项测试按 RED→GREEN 完成；adapted `9 files / 68 PASS`、typecheck/build 通过。根 `pnpm.cmd run check` 为 `34 files / 113 PASS + 3 platform skip`，Python Bridge `15/15`，四套真实 Profile 与根生产构建全部通过。
- 正式 `3080` 在 `390 / 760 / 1024 / 1280 / 1440` 五档宽度均为 `14px / 24.08px`、页面级横向溢出 `0`、控制台 `0 error / 0 warning`。macOS 最终实机项仍由接收设备完成。

## 2026-08-30 三轮复核后加固与最终 Windows 收口

- 内容/推理投影、会话级文字与图片草稿、长历史和大目录分页、共享弹层、记忆项目键、Product Health 服务边界均已按公开 Provider/Consumer 合同修复；壳没有新增竞争真源。
- 插件清单主路径按用户能力分组并隐藏内部术语；手机消息标记和失败恢复动作达到 `44px` 命中区；设置 Escape 关闭后焦点回到稳定“设置”入口；移动弹层 Escape 不再误关底层侧栏。
- 消息定位点击使用与激活算法相同的 `34%` 阅读线，并修复整数 `offsetTop` 与小数滚动位置造成的 `1px` 高亮竞态。正式 `1280×900` 实测点击第 3 条后 `aria-current=3`，目标与阅读线差 `0.02px`。
- 新鲜门禁：adapted `9 files / 67 PASS`、typecheck/build 通过；DSH 通用设置 `18/18`、typecheck/build 通过；根 `pnpm.cmd run check` 为 `34 files / 113 PASS + 3 platform skip`，Python Bridge `15/15`，四套真实 Profile 与根生产构建全部通过。
- 正式 `3080` 新页面复验 `390 / 760 / 1024 / 1280 / 1440` 五档宽度，页面级横向溢出为 `0`；插件、设置焦点、草稿隔离、上弹菜单、斜杠命令、回底和消息定位均已真实操作，控制台 `0 error / 0 warning`。
- macOS 最终实机、系统权限、多显示器、跨屏拒绝边界和真实桌面动作仍为 `PENDING`，由接收设备执行；当前不是提交、合并、发布或上线批准。

## 2026-08-29 双状态线稿与标题最终收口

- 空状态四项内容已经归为单一 `stage-cluster`，整体按屏幕高度下移 `14–22px`；`1280×720` 实测视觉中心从可用区 `49.6%` 调整为 `52.7%`，不会再各自漂移。
- 对话开始前“小蛇”旁的官方线稿不改 SVG、透明度、滤镜或描边，尺寸限制为 `min(280px,42%)`，再独立向右偏移 `32–56px`、向下偏移 `10–18px`；五档真实浏览器几何合同保证它不会重新挤入主字或溢出页面。
- 正式对话右下水印与开始前线稿是两个独立元素：正式水印尺寸不变，`1440×900` 仍为 `270×270`，只把底部间距由 `45px` 改为 `14.4px`，即下移 `30.6px` 且不碰输入器。
- 已命名、未命名与新会话标题全部统一为 `18–23px / 500` 的轻雾面元信息；只有自动标题额外使用等宽数字。五档视口无横向溢出，亮/暗模式 console 为 `0 error / 0 warning`。
- 新鲜门禁：正式 adapted `60/60`、typecheck/build 通过；根 `pnpm.cmd check` 为 `34 files / 112 PASS + 3 platform skip`，Python Bridge `15/15`，根 typecheck/build 通过。完整数据追加在 `docs/evidence/native-shell-legacy-adapted/2026-08-29-ux-repair-acceptance.md`。

## 2026-08-29 正式壳三轮体验审计修复

- 运行态输入器已同时提供“停止”和“调整方向”，分别走公开 `stopRun()` 与 `sendTurn(..., mode: 'steer')`；移动端不再隐藏关键动作文字。
- 斜杠命令接入真实 Session Command：`/compact` 不会被发送给模型；命令页保留 `/new`、`/stop`、`/fork`、`/status`、`/memory`、`/capabilities`、`/plugins`，没有底层契约的 `/resume` 未伪造。
- 设置中心完成信息架构收口：插件与能力按用户用途命名和去重，服务商新增不再默认 Amazon Bedrock，记忆可在同一草稿上放大编辑；设置结构使用稳定的 `data-xs-settings-*` 契约。
- 主题改为消费 DSH Theme 单一真源；长标题、历史错误、折叠推理、会话去重标题、消息定位、低对比回底按钮、权限/思考强度上弹层、键盘/读屏/触控与品牌水印均完成修复。所有会话标题最终统一为较小、较淡的无衬线雾面元信息，自动标题只额外使用等宽数字。
- 新鲜门禁：正式 adapted `60/60`、Runtime Contract `19/19`、Runtime Provider `19/19`、Task Timeline `3/3`、DSH 设置 `524/524` 通过；相关 typecheck/build 均通过。
- 正式 `3080` 浏览器覆盖 `1440×900`、`1280×720`、`1024×768`、`760×900`、`390×844`，页面横向溢出和标题碰撞均为 `0`；亮/暗主题、设置、插件、记忆、上弹菜单、斜杠命令和键盘标签切换通过，控制台 `0 error / 0 warning`。
- 完整凭证：`docs/evidence/native-shell-legacy-adapted/2026-08-29-ux-repair-acceptance.md`。macOS 实机、系统权限、多显示器和真实桌面动作明确保持 `PENDING`，由接收设备执行。

## 2026-08-29 正式交互、插件清单与视觉细节收口

- 设置中的插件清单不再逐条暴露含义不明的 Loader 名称。当前真实快照的 `152` 个模块、`178` 个运行实例被整理为 `37` 张用户能力卡和六个分组；可见标题无重复，运行基础默认折叠，技术模块与每个实例 ID 仍可展开和搜索。
- 正式输入器在模型运行时显示“停止生成”和“调整方向”：停止走公开 `cancel()`，运行中补充走 `steer`，空闲普通发送走 `queue`；停止处理中防重复点击并保留错误反馈。
- 左侧消息定位已取消连续竖线，改为互不相交、左右轻微错位的独立玉色鳞片；当前、悬停或键盘聚焦时才展开为短玉条并显示摘要。回到最新消息按钮为 31px、默认透明度 `0.48` 的按需浮层，点击回底后自动消失。
- 官方线性蛇标只调整亮/暗主题可见度；非空正式对话右下恢复同源官方轮廓水印，并按用户追加意见仅用 CSS 放大约 `1.25×`。官方 SVG、渐变、线条滤镜和两处 morphology `.4` 均未修改。
- 新鲜门禁：adapted `52/52`、Runtime Provider `18/18`、根 `112 PASS + 3 platform skip`、Python `15/15`、DSH 设置专项 `9/9`，全部相关 build/typecheck 与 DSH Profile 解析通过。正式 `3080` 插件页实测 `0` 重名、`0` 溢出，浏览器日志为空。
- 证据与最终截图见 `docs/evidence/native-shell-legacy-adapted/2026-08-29-interaction-plugin-visual-refinement.md`。

追加视觉复验已更新到 `docs/evidence/native-shell-legacy-adapted/2026-08-29-ux-repair-acceptance.md`：正式 adapted `60/60`、typecheck/build 通过；五档浏览器已复核分离式定位节点、正式水印下移、开始前线稿独立缩放与错位、全部标题统一雾面弱化、无横向溢出及浅/深主题控制台。

## 2026-08-29 macOS 最新进展接回 Windows

- 输入归档 SHA-256 `d6a523aadaa707beccb7745ee68e2affb521a7f3ca21e96cece5325c1a8c97ff` 与 sidecar 一致；来源清单为 10,719 个文件、208,225,180 bytes，内容哈希、三个 HEAD 和 Git 对象通过。
- 相对 Windows 正式工作区，来源新增 51、修改 42、删除 0。同步前 42 个同名旧文件已保存到 `C:\Users\example\Downloads\XS交接\本机同步回滚-20260829-031232`；同步后 93/93 路径哈希一致，三套 `.git` 和本机状态未覆盖。
- 官方 Windows CheckOnly 与正式接收安装均退出 0。正式 adapted 49、terminal-client 14、runtime-contract 18、runtime-dsh-provider 18 项通过；根门 112 项通过/3 项平台跳过，Python 15/15，typecheck/build、真实 Profile、Phase 7 和篡改回归均通过。
- 正式 3080 为“小蛇 0.2.0 / Bridge ready / win32”；`xiaoshe-doctor` 为 `ready=true / failed=0`，唯一警告是 Developer Mode 未开启。浏览器控制台 0 error / 0 warning，真实三条消息索引的悬停摘要与点击跳转通过。
- 完整视觉矩阵覆盖 2269、1440、1024、740、390；自适应矩阵覆盖 1180、2560、1920、1280、740、390、375 及亮/暗设置页，均 PASS。Windows 验收暴露并修复了视觉脚本硬编码 macOS 日志路径的问题。
- 接收凭证：`docs/evidence/cross-device-sync/2026-08-29-macos-to-windows-receipt.md`。工作现场侧舱的真实浏览器动作仍必须由真实结构化 surface 事件触发，不能用伪数据替代。

## 2026-08-28 长会话“我的消息索引”与换机收口

- 正式 adapted 壳的长会话索引已改成按**用户实际发送的消息数量**增长：每条消息占一个固定行，未达到上限时不再拉成整列；高度上限为 `min(48vh, 420px)`，超过后只在索引内部滚动。
- 标记悬停或键盘聚焦时会突出显示，并在滚动裁剪区外显示“我发送的第 N 条”及消息摘要；点击仍平滑跳转到对应用户消息。切换会话或滚动索引会关闭旧预览。
- 隔离浏览器验收：3 条用户消息时索引高 `52px`、无滚动；40 条时封顶 `420px`、内容高 `718px`、可内部滚动；预览位于索引滚动容器之外，不会被裁切。
- 新鲜完整门禁：根工程 `32 files / 108 tests PASS + 6 platform skip`，Python 桌面桥 `15/15`，TypeScript 类型检查和生产构建通过。Phase 7 重建为 10 包、`254933` 字节、8 个端点 200、源/目标路径泄漏 0、10 个目标本地 override、sentinel 未变化。
- 正式 `3080` Client 与本地 `packages/native-shell-legacy-adapted/lib/client.js` 的 SHA-256 同为 `7ee7b831653f7031adefc59239b9daea0312cd2b1494c2158a9ae05a300fb6b3`。接手设备不要重做此功能，只需刷新正式页并在目标显示器复验索引长度、内部滚动、悬停摘要和跳转。
- 换机预检发现根目录启动/停止入口曾缺少 macOS executable bit；现已恢复全部 `.command` 入口执行权限，并让 macOS 打包器在暂存和真实解包后各检查一次，避免把 Finder 无法双击的包交给下一台设备。
- 结构化验收记录见 `docs/evidence/native-shell-legacy-adapted/2026-08-28-message-index-acceptance.md`。

## 2026-08-28 终端与工作现场侧舱 V1

- 终端客户端补齐 EOF 正常退出、未答问题取消、审批在输入消失时明确拒绝，以及排队模式只接受当前回合结束事件。真实 `deepseek-v4-pro` 共享运行时返回精确文本 `XS_TERMINAL_SHARED_RUNTIME_OK`，持久恢复 34 个事件；本轮未打开界面。
- 新增会话级 `WorkSurfaceRegistry`。DSH Provider 只把结构化工具结果投影为 `web / file / image / video / pdf / terminal / desktop` 现场，不扫描文件系统、不猜助手文本、不复制 Session Log。
- 正式 adapted 中央任务区现在可按需打开“工作现场”：多标签、固定、关闭、刷新、复制安全来源、外部打开、拖动/键盘调宽、会话级恢复与窄屏覆盖均已编码；关闭时恢复老版三栏。
- 网页默认仅观看；只有无凭据的精确 loopback URL 可内嵌，其他来源明确降级。V1 不包含“小蛇接管操作”，也没有新增任意文件读取端点。
- 新鲜后台门禁：全工作区 19/20 typecheck/build 通过；adapted 46、runtime-contract 17、runtime-dsh-provider 16、terminal-client 14 项通过；根门为 32 files / 108 tests PASS + 6 platform skip，Python 15/15。
- Phase 7 搬移复验为 10 包、248976 字节、8 端点 200、生成源失效、源/目标路径泄漏 0；正式离线目录已重建。详见 `docs/evidence/work-surface-dock-v1/acceptance.md`。

## 2026-08-27 Windows 最新进展接回 macOS

- 输入包 `/Users/example/Downloads/XS交接/XS-完整交接包-20260827-061411.tar.gz` 为 10,922,566,489 字节，SHA-256 `17eed8ee4c6c9e980bf693c29bece86ccbae20ca8adccfc0842c55211e18c768` 与 sidecar 一致。异常体积来自包内递归携带 9 个旧交接包；提取时只排除这些旧包和可再生成目录，原始输入包未改动。
- 本地上一版 10,552 文件与输入版 10,668 文件逐路径比较：新增 116、修改 29、删除 0。同步严格限制在这 145 条路径，未覆盖三套 `.git`、依赖、凭据、会话或运行状态；145/145 哈希匹配。29 个旧文件保存在 `/Users/example/Downloads/XS交接/本机同步回滚-20260827-125244/originals`。
- 输入载荷的 10,668 个文件内容哈希全部通过；临时 Windows 工作树在 macOS 只保留顶层和 Legacy 的 executable-bit/symlink status 指纹差异。同步到现有 macOS 三仓库后重新生成本机清单，文件、Git 对象、HEAD 与工作树均通过。
- 无界面全门禁退出 0：18/19 个工作包 build/test/build/typecheck 通过；正式 adapted 41/41；根 32 files PASS + 2 platform skip、106 tests PASS + 6 platform skip；Python Bridge 15/15；插件治理、V6、正式壳和可重定位真实 Profile 均通过。
- Phase 7 在本机重新生成并真实搬移：10 包、232392 字节，源目录失效后启动成功，8 个端点 200，源/目标路径泄漏均为 0，10 个目标本地 override，sentinel 未变化。
- Windows 打包器已增加独立的 `XS-完整交接包-*` 通配防递归，并新增回归断言；下一次仍需在 Windows 实机创建小型交接包确认 tar.exe 行为。完整接收证据见 `docs/evidence/cross-device-sync/2026-08-27-windows-to-macos-receipt.md`。
- 按用户要求，本次接收、同步和验证没有启动小蛇界面或浏览器。

## 2026-08-27 跨设备同步最终闭环

- Downloads 来源 manifest 的 10,552 个文件已逐路径映射到当前工作区：10,523 个逐字节相同、29 个同步后继续推进、0 个缺失。来源归档 SHA-256 与 sidecar 匹配；Windows 新鲜来源校验只保留已记录的顶层 executable-bit 工作树指纹差异。
- 29 个推进路径已复核为 Windows Junction 安全备份、正式 adapted 的记忆/设置/长会话/字体/自适应实现、Product settings-only 装配、对应回归测试、浏览器验证器、说明和更新截图，没有发现来源能力被当前版本覆盖掉。
- 正式 3080 自适应专项与完整交互回归均 PASS；`验证交接-Windows.ps1 -Full` 新鲜退出 0：18/19 个工作包 build/test/build/typecheck、adapted 41/41、根 111 项、Python 15/15、真实 Profile、Phase 7 和 DSH dump 全通过。
- `xiaoshe-doctor` 返回 `ready=true / failed=0 / warnings=1`；唯一警告是 Windows Developer Mode 未开启。交接文档旧的 37 项计数已更新，并补齐 `交接工具/故障恢复.md`。
- 详细结构化证据见 `docs/evidence/cross-device-sync/2026-08-27-final-handoff-audit.{md,json}`。

## 2026-08-27 Windows 接管与接收器修复

- `C:\Users\example\Downloads\XS` 的最新进度已按来源清单增量同步到本工作区：329 个文件、25,503,874 bytes；44 个同名旧文件先备份，285 个新增。备份位于 `C:\Users\example\Desktop\XS-同步前增量备份-20260827-000253`，三个 `.git` 均未覆盖。
- 来源归档 SHA-256 `f94c6ed67fe8f67167d9c3a8415abe8ec3ccbdaf7e34ac2c2ebe211f2f42571c` 匹配。逐文件哈希、三个 HEAD 和 Git 对象通过；来源清单在 Windows 只剩 macOS executable-bit 导致的顶层 status 指纹差异。
- Windows 接收器真实运行发现 Profile `node_modules` Junction 递归备份故障；现只备份持久 Profile 文件，并由真实 Junction 测试保护。adapted 来源测试也已忽略 `.DS_Store` 并统一跨平台路径分隔符。
- 接收器从入口完整重跑退出码 0：18 个工作包 build/test/typecheck 通过，根 Vitest 111 项通过、1 项平台条件跳过，Python 15/15；插件治理、V6、正式壳和 Phase 7 真实 Profile 均通过。
- `xiaoshe-doctor` 返回 `ready=true`、`failed=0`；正式 `web` Profile 和 Windows `s` / `xiaoshe-doctor` 入口已安装。详细凭证见 `docs/evidence/cross-device-sync/2026-08-27-downloads-to-desktop.md`。

## 2026-08-27 正式界面精修、设置与记忆收口

- 水印 morphology 半径从 `0.75` 收细到 `0.4`，其余品牌几何不变；权限与推理档位均改为有说明、状态和危险语义的自定义向上弹层，不再使用浏览器原生下拉或闪电图标。
- 左下角只保留插件提供的齿轮“设置”；重复导航与常驻 Ctrl K 文案已删除，快捷键能力仍在。右栏显示名改为“状态 / 记忆 / 能力”，内部 `system` 路由保持兼容。
- 记忆页通过公开 Memory 服务支持新建、修订、遗忘、恢复、全局/项目范围和 revision 冲突后的草稿保留，不再是只读摘要。
- 设置壳只聚合插件贡献。模型、插件和能力方案仍由各自插件拥有；正式适配壳只贡献它真实拥有的权限边界、快捷键、诊断和输入发送方式。通知、自动更新等未接底层能力的开关未出现。
- 验收中真实 Profile 抓到 `ui-conversation` 被误当“设置行”启用会破坏单一产品壳边界；现已重新禁用整套通用会话表面，由小蛇输入器自身贡献并执行“Enter 发送 / Ctrl/Cmd+Enter 发送”。
- 新鲜门禁：适配壳 8 files / 41 tests、Product Bundle 3 tests、Memory 13 tests 均通过；根 `pnpm.cmd run check` 为 33 files PASS + 1 platform skip、111 tests PASS + 1 platform skip、Python 15/15、typecheck/build PASS。
- 正式服务 `127.0.0.1:3080` 重启后返回“小蛇 0.2.0 / Bridge ready / win32”；全新 Chromium 页 0 error / 0 warning。1440、1024、720、390 和 412 设置页复验均无页面级横向溢出。详细证据见 `docs/evidence/native-shell-legacy-adapted/2026-08-27-refinement-acceptance.md`。

## 2026-08-27 标题栏比例与左右视觉重量收口

- 中央标题栏从双行封面式结构改为同轴紧凑布局；标准桌面实测 `138px → 92px`，标题与运行事实中心差 `约 35px → 0px`。标题字体、颜色、字号和横向网格没有改变。
- 左右栏仍保持 `232px / 292px` 及既有拖动边界。右栏通过实体浅卡面、深浅文字层级、分区引导线和 `2px` 状态线增加重量，没有扩大栏宽或堆叠彩色装饰。
- 状态颜色继续服从 Provider：`healthy → ok`，`lost / delayed / backoff → warn`，其他 Heartbeat 状态保持中性；浏览器实际确认当前 `healthy` 卡为 `data-tone=ok`。
- 亮色与墨玉主题均复验；1920、1440、1024、760、390 五档视口无页面级横向或纵向溢出，最终控制台 `0 error / 0 warning`。
- 新鲜门禁仍为适配壳 8 files / 41 tests、根 111 tests + 1 platform skip、Python 15/15、全部相关 typecheck/build PASS。详细尺寸、RED/GREEN 证据与 SHA-256 见 `docs/evidence/native-shell-legacy-adapted/2026-08-27-refinement-acceptance.md`。

## 2026-08-27 全页比例、自适应与设置层级收口

- 信息架构复核后保持四个清晰职责：左栏是对象导航，中间是当前任务权威面，右栏是当前会话实时事实，设置是跨会话默认项与治理。当前控制与默认设置不是重复功能；本轮只移除窄屏重复呈现，没有删除服务或插件能力。
- 紧凑桌面右栏 overlay 断点由 `1080px` 提升到 `1180px`。`1180×720` 中间工作区实测由约 `656px` 增至 `970px`，标题栏 `92px → 80px`，输入器约 `166px → 147px`；`2560×1080` 输入器上限由 820px 放宽到 900px。
- `1280×1024` 继续保持 `232 | 756 | 292`，`1920×1080` 继续保持 `256 | 1344 | 320`；既有桌面栏拖拽、键盘调整、复位和持久化未改。
- `740×900`、`390×844`、`375×667` 下顶部只保留必要状态，底部隐藏长会话 ID，任务/状态/主题/权限/推理/发送触控目标均不少于 40px。左右抽屉和全部真实功能仍在。
- 设置遮罩现在覆盖三栏，桌面高度封顶 680px；打开后从桌面缩到手机仍保持可见。390px 下三个主题按钮同排，设置页亮/暗背景分别与小蛇壳完全一致；这里只重绑视觉 token，设置数据与所有权仍由各插件提供。
- 真实浏览器专项覆盖 1180、2560、1920、1280、740、390、375 七档及亮/暗设置；既有 2269、1440、1024、740、390 五档完整交互回归也通过。页面级溢出为 0，最终正式 `3080` 亮色 1440×900 控制台 `0 error`。完整证据与截图见 `docs/evidence/native-shell-legacy-adapted/2026-08-27-refinement-acceptance.md`。

## 2026-08-26 晚间正式界面交互收口

- 左栏项目现在按工作区作为一组折叠；数字计数已经删除，项目标题前使用文件夹语义标识，右侧 `+` 与 `…` 对齐。项目菜单通过公开 Product 服务支持改名和安全移除；项目内会话菜单支持改名和安全移除，不再只是增加会话。
- 输入器已分成两层：上层承载文字与图片预览，文本框随内容增高并在达到上限后内部滚动；下层集中附件、真实模型名称、推理档位、权限档位和发送。模型与推理保持独立控制，权限入口已经恢复，工作区名称不进入输入器。
- 输入 `/` 会打开真实命令列表并按前缀过滤；选择命令走公开命令动作，不把命令文本发给模型。左下角重复的命令入口已移除。
- 桌面左右栏已补齐真实拖动调宽：左栏 `184–380px`、右栏 `248–480px`，同时受中央任务区最小宽度约束；支持键盘方向键、Home/End、双击恢复默认值，宽度写入本机偏好。进入 overlay 断点后不伪装成可拖动栏，回到桌面宽度时恢复用户设置。
- 最新完整视觉证据位于 `artifacts/native-shell-legacy-adapted-resizable-rails-final/`，包含宽屏、标准桌面、真实拖宽、项目/会话编辑菜单、两层输入器、图片输入、斜杠命令、平板与移动端状态。
- “工作现场侧舱”在当时只完成可行性判断；已于 2026-08-28 完成本地 V1。它采用 `会话栏 | 任务/对话 ↔ 可收起工作现场 | 状态/记忆/系统`，没有增加永久第四栏；真实浏览器接力边界见 `交接工具/下一步-工作现场侧舱.md`。

## 2026-08-26 老版母版完整适配与正式装配

- 新包：`packages/native-shell-legacy-adapted/`。`ui-baseline/` 与 `runtime/xiaoshe-legacy/ui` 均为 57 个文件，路径与内容整树 SHA-256 同为 `2ea18a7cb27dd5fb5f407a658de75e5751dec28f6fae17f51979e05b9645e17f`；原版未直接修改。
- 工作 UI 只保留老版视觉资产/样式，不携带退役 HTML、旧 JS 协议或 service worker。品牌只使用正式 `assets/snake.svg`（SHA-256 `3a919a69c3b6f425545957aacaccd0199364a233bb3a0f4db827f3b5d1efdd75`）与 `assets/icon-256.png`（SHA-256 `ac2b7c8f62f571c62a993c23d2ff115546c9b7b52a1bd3c01e7f13daea448b8a`）；空态水印沿用老版形态学描边算法，保持线稿。
- 三栏与老版视觉保留；会话、工作区、模型/推理档位、时间线、全部审批、上下文/cache、凭证、Heartbeat、记忆和插件治理均接入公开 Product 服务。工作区名称只在左栏显示，标题副行、输入框和右侧系统页不再重复；输入器显示真实模型名称，推理档位与权限仍是独立控制。伪项目错误、固定模型、伪 token/工具计数、伪审批档位和装饰入口已删除。
- 左上角蛇标已按老版官方内联几何、瞳孔 mask 与四色流光恢复；仍以 `runtime/xiaoshe-legacy/ui` 为唯一品牌来源，未新画 `S`。
- 1440px 保持老版 `232 | 1fr | 292`，2K 超宽屏使用 `256 | 1fr | 320`，1180 及以下右栏改为 overlay，760 及以下改为双抽屉。多档浏览器无页面级溢出，左右收放、命令面板和状态/记忆/能力均已真实操作。
- 正式 `@xiaoshe/product-bundle` 现在直接装配 `@xiaoshe/native-shell-legacy-adapted`；历史比较 overlay 也已改为只停用当前正式 adapted seat，不修改各候选 UI。
- 正式 Profile add/start/remove/restart 通过：安装态 root/client/provider/brand/heartbeat/memory/governance 均 200，移除后 Product 端点均 404，Heartbeat `idle → running → healthy`，重启恢复 healthy，sentinel 不变。
- 最新 Phase 7 离线闭包已重建：10 包、232392 bytes、8 端点 200、生成源目录已失效、零生成源路径泄漏、零目标 Profile 源路径泄漏、10 个目标机本地 override、sentinel 不变。
- 结构化证据与截图：`docs/evidence/native-shell-legacy-adapted/`。

## 2026-08-26 跨设备同步与 Windows 接手机验

- 输入清单：`C:\Users\example\Downloads\XS\交接工具\完整性清单.json`，10,228 个文件、166,036,914 bytes，SHA-256 `7CF3B940807D5E3011632CA9519077F6CB8DD1E4E27156E828C980BF09A1E8CB`。
- 本机同步前清单：10,173 个文件、165,724,925 bytes，SHA-256 `2846028C233016EAC02034288C02E00EC44CA35B4534F2B0288FB380BEBB7D24`。
- 只复制清单差异中的 79 个普通文件；没有删除文件，也没有复制 `.git`、`node_modules`、构建缓存或运行状态。被覆盖的 24 个旧文件和同步前清单保存在 `C:\Users\example\Desktop\XS-同步前增量备份-20260825-235503`。
- 输入清单的全部文件哈希、三个 HEAD 与 `git fsck` 均通过；只有输入清单记录的顶层和 DSH Git status 指纹在 Windows 不相等，原因是 macOS/Windows 的 executable-bit 与 symlink 表示差异，未把它误报为载荷损坏。
- Windows 本机复验发现并修复 Phase 7 安装器的真实可移植性问题：含空格路径不能逐项穿过 DSH 的 Windows shell 参数转发。安装器现先写入经过哈希校验的 `file:` 依赖，再以无路径参数的一次离线安装完成 DSH 对账；原 DSH 独立工作树未被修改。
- 修复后真实搬移验收通过：目录 A 生成、复制到含空格目录 B、令 A 不可用后，10 个包安装、DSH 启动、7 个 HTTP 端点、10 个本地 override、零源路径泄漏和 sentinel 不变全部通过。
- 详细同步凭证见 `docs/evidence/cross-device-sync/2026-08-26-downloads-to-desktop.md`。

## 2026-08-26 Windows → macOS 接力同步与本机复验

- 同步来源为 `/Users/example/Downloads/XS`，目标为 `/Users/example/Desktop/XS`；来源清单记录 10,268 个文件、166,752,742 bytes，三个 Git HEAD 与全部普通文件哈希通过。来源清单中的根/legacy status 指纹在 macOS 因 executable-bit 与 symlink 表示不同而不相等，未误判为载荷损坏。
- 同步只覆盖交接清单定义的源码载荷；`.git`、`node_modules`、构建产物、凭据、会话、日志、缓存、运行状态和旧压缩包未覆盖。被替换或移出的目标文件保存在 `/Users/example/Desktop/XS-同步前增量备份-20260826-100746`。
- Node `v24.19.0` / pnpm `11.7.0` 本机门禁通过：16 个 workspace package 共 163/163 项测试通过，全部已声明 build/typecheck 通过；根 Vitest 32 个文件通过、1 个条件跳过，106 项通过、5 项平台条件跳过；Python Bridge 15/15 通过。
- V6 新鲜隔离 Profile 位于 `/tmp/xiaoshe-candidate-v6-macos-20260826`，当前查看地址为 `http://127.0.0.1:60686/`。Root、V6 Client、正式品牌与 Heartbeat 均返回 HTTP 200；在线品牌与 `runtime/xiaoshe-legacy/ui/assets/snake.svg` 的 SHA-256 同为 `3a919a69c3b6f425545957aacaccd0199364a233bb3a0f4db827f3b5d1efdd75`。
- 当前会话没有可调用的浏览器控制实例，因此没有把 HTTP 200 冒充 macOS 视觉通过。下一步应在目标显示器实际打开 60686，完成浅色/墨玉、左右栏、七档尺寸和细节动效的主观检查。

## 三棵必须原样保留的 Git 工作树

| 工作树 | 分支 | HEAD | 当前状态（展开全部未跟踪文件） |
| --- | --- | --- | --- |
| `XS` | `codex/xiaoshe-native-shell` | `b10ef68d13203ede11eefcd3dabbe429c85d8b82` | 当前 macOS 清单冻结 317 个过滤后的 dirty entries；Windows 曾显示 421 个未过滤展开项，差异来自清单排除项及跨平台表示 |
| `runtime/DSH` | `master` | `141eb6fef83422698aef7a981029e843e8161534` | 当前 macOS 清单冻结 75 个 dirty entries；文件载荷未改 |
| `runtime/xiaoshe-legacy` | `codex/plan05-effects-plan06` | `634d5671f9bf4ecedc5dc7404ee447ddc9e3a44d` | 490 个由交接清单冻结的 dirty entries；正式品牌、兼容层和未提交现场均保留 |

精确文件哈希、大小和 Git 状态以同目录最新 `完整性清单.json` 为准。禁止在任一工作树运行 `git reset --hard`、`git clean`，也不要擅自规范 Windows executable-bit 或 symlink 差异。

## 本轮完成内容

### Phase 4：记忆产品能力

- 独立 `@xiaoshe/memory` Provider/Client、Profile 所属持久状态、scope/forget/revision 语义和受限公开投影已接入 Product Bundle。
- 原生壳通过公开 Client 服务消费记忆和上下文治理，不复制 DSH Session Log，也不把迁移/压缩职责塞进 `AgentRuntimeSession`。
- 独立 Profile、浏览器挂载和生命周期测试证据位于 `docs/evidence/native-shell-phase-4/`。

### Phase 5：完成凭证与真实后台检查

- `@xiaoshe/verification-policy`、策略化 Completion Receipt、Heartbeat v2 持久检查账本、DSH Jobs 协调器、租约/恢复/暂停/退避和受保护 HTTP API 已接入 Product Bundle。
- 后台执行以 DSH Jobs 为权威执行面；Heartbeat 只保留调度/检查事实，重启会恢复中断租约。
- 最终新鲜 Profile 暴露了 settings writer-lock 短暂竞态。存储边界现在只对精确的 writer-lock timeout 做 `50ms`、`150ms` 两次有界重试；其他错误立即失败，持久孤儿锁不会被静默删除。
- V6 新鲜 Profile 又暴露 HTTP 路由先于 Heartbeat 异步恢复完成的启动竞态：`run_now` 可能短暂返回 500。控制动作现在会等待唯一的启动 Promise；新增回归测试后 Heartbeat 19/19、根真实 Profile 与 V6 Profile 均通过。
- 证据位于 `docs/evidence/native-shell-phase-5/`。

### Phase 6：受控插件治理

- `@xiaoshe/plugin-governance` 已完成 Host 候选审计、大小/路径/manifest 约束、SHA-256 事实绑定、一次性确认、非活动 `xiaoshe-managed-*` Profile 限制和 Profile 所属事务账本。
- 变更只通过官方 DSH CLI argv 执行，覆盖 install/update/remove、dump/boot/功能健康门、显式恢复、partial-health、失败逆操作和 rollback-failed 残留记录。
- 真实隔离 Profile 已证明安装、健康、卸载、恢复和失败回滚；浏览器证明 prepared 状态可见且确认前不会创建目标 Profile。
- Host 插件仍是进程内受信任代码，`osSandboxEnforced` 明确为 `false`。
- 证据位于 `docs/evidence/native-shell-phase-6/`。

### Phase 7：跨设备可重定位离线工件

- `交接工具/离线工件/xiaoshe-product/` 包含 10 个锁定 Product tarball、manifest、独立安装器和说明；每个包记录安装顺序、大小与 SHA-256。
- 真实验收先在目录 A 生成，再复制到包含空格的目录 B，并使 A 不可用；随后只依靠 B 安装新 Profile、启动 DSH，并验证 8 个 Product 端点均返回 HTTP 200。
- 搬移工件和目标 Profile 中生成源路径泄漏均为 0；10 个运行依赖均解析为目标机本地 override，交接 sentinel 未变化。
- `pnpm run accept:phase7` 可重复执行完整搬移验收；证据与真实边界见 `docs/evidence/native-shell-phase-7/README.md` 和 `relocation-report.json`。

### 真实模型、审批回放与 macOS 真实动作

- 正式 `127.0.0.1:3080` 服务使用 `deepseek-official / deepseek-v4-pro` 完成真实回合；审批请求在事件 WebSocket 断开、重连后保留同一 `rpcId` 与 `approvalId`，以 `allowed-once` 解决，并持久记录 `approval/asked` 与 `approval/decided`。
- 模型获批后真实 bash 工具写入精确证据，回合以 `completed` 结束。验收脚本不包含密钥；`pnpm run accept:real-model-approval` 会产生真实调用费用，因此不进入默认 `check`。
- 同一受控会话的 `/compact` 已调用 DeepSeek V4 Pro 摘要 6 个历史项（约 822 tokens），持久事务序号为 start 225、summary 226、checkpoint 227、end 228、command done 229；checkpoint 与 shadowed range 精确匹配。
- 正式完成凭证为 schema v2、`outcome=verified`、`unverified=[]`，包含真实 bash 工具成功事实和 `allowed-once` 审批；可用 `pnpm run accept:real-compaction-receipt` 无重复计费复核已有成功。
- macOS 桥在受控 AppKit 窗口中解析唯一 AX 按钮，复核局部视觉后通过 CGEvent 发送真实点击；动作前后截图变化、目标应用回调写入均通过。动作能力关闭时，同一调用以 `ACTIONS_DISABLED` 拒绝。
- 过程中修复了 macOS AXPress 可能返回成功但不触发 AppKit 回调的假阳性：macOS 元素动作保留 AX/局部像素新鲜度门，最终注入改走会校验鼠标到位的 CGEvent；Windows UIA 路径保持不变。
- 当前双屏 Mac 的 fail-closed 路径已真实验证：副屏 7 个 AX 元素从主屏观察表中过滤，不可见 element ID 不发送动作，主屏外坐标以 `INVALID_PARAMS` 拒绝，副屏目标没有业务写入。跨屏动作仍未实现。

### 独立小蛇界面候选 V6 旧版视觉继承（保留的历史候选）

- 新增 `packages/native-shell-candidate-v6/` 与 `packages/native-shell-candidate-v6-bundle/`；V6 有独立 module/seat、品牌端点、Profile verifier 和覆盖 patch，只停用候选 Profile 中的正式 root seat，不引用或覆盖 V5。
- 视觉直接继承 `runtime/xiaoshe-legacy/ui`：默认云白薄荷、墨玉暗色、旧字体栈与颜色 token、桌面 `232 | 1fr | 292`、左右 48px 折叠、26px 状态栏、720px/20px 输入器、36px 发送键和左右一致的 24×24 折叠按钮。
- V6 只消费公开 `agentRuntimeSession/sessionCatalog/taskTimeline/contextGovernance/userApproval/memoryLifecycle/pluginGovernance/slots` 服务；DSH Session Log 与各 Provider 仍是事实真源，没有第二日志、私有 DSH API 或任意 shell 安装入口。
- 状态/记忆/系统页签、能力中心与插件治理使用旧版轻量 inspector/modal；确认 token 不进入 DOM，Host 插件无 OS 沙箱的现实边界明确展示，本次未执行真实插件变更。
- 真实浏览器验收覆盖 2269×1214、1920×1080、1440×900、1024×768、760×900、390×844、320×568；全部无横向溢出，亮/暗主题和左右 overlay 均可用，最终导航控制台 0 error / 0 warning。
- 验收中修复移动浮层误用桌面折叠逻辑而遗留空遮罩的问题；浮层内收缩现在直接关闭，桌面仍折叠至 48px。回归测试覆盖移动关闭、桌面折叠及跨断点恢复。
- V6 证据见 `docs/evidence/native-shell-ui-candidate-v6/acceptance.md` 与 `browser-acceptance.json`；Windows 临时查看地址 `http://127.0.0.1:58818/` 只属于历史验收现场，不进入交接包。正式入口已经迁移到独立 `@xiaoshe/native-shell-legacy-adapted`。

### 独立小蛇界面候选 V5 品牌与动效（保留的历史候选）

- 新增 `packages/native-shell-candidate/` 和 `packages/native-shell-candidate-bundle/`；候选覆盖层只在专用 Profile 中停用原 root seat 并启用候选 seat，不改变 Product Bundle 服务组合。
- V1 浅色视觉已被用户否决并保留为历史证据；V2 恢复了熟悉的深色 `会话 | 任务过程 | 工作台` 三栏与 `状态 | 记忆 | 系统` 右栏。V3 收敛了字号、圆角、渐变、阴影、会话选中态、工作台层级和输入器细节。V4 针对用户 `2269 × 1214` 显示环境补齐宽屏/超宽屏比例和空态垂直节奏。V5 修复候选加载后的浏览器 title/favicon，并从正式原版恢复四色 9 秒流光、状态呼吸、消息进入与正式图标轮廓，不改变信息架构。
- 候选消费真实会话、搜索、发送、审批、上下文、记忆、完成凭证、Heartbeat 与插件治理服务；插件管理只做受管意图、事实审计和一次性确认，不生成 shell 命令。
- V5 保留 V4 自适应契约：标准 1440px 为 `236 | 912 | 292`、内容上限 800px；1920px 为 `264 | 1328 | 328`、内容上限 1040px；用户 2269px 视口自动命中 `264 | 1677 | 328`、内容上限 1080px。标题、任务流与输入器共用内容网格，空任务流按剩余空间居中；`≤1180 / ≤860 / ≤620` 的窄屏降级和左右收放仍保留。V5 已通过真实 Profile/HTTP/哈希探针，但尚未获得用户在目标显示器上的视觉批准。
- 插件健康 fixture 已真实审计并准备一次性确认，但未按下最终确认；原始 token 未进入 DOM，`xiaoshe-managed-browser` 未创建。
- V1/V2/V3/V4/V5 均保留为历史证据；V5 证据位于 `docs/evidence/native-shell-ui-candidate-v5/`，实施记录为 `docs/superpowers/plans/2026-08-25-xiaoshe-native-shell-ui-candidate-v5-brand-motion.md`。
- 历史 `60448` 与 `/tmp/xiaoshe-candidate-preview-v5.fImT8i` 只是当时的临时验收现场，不进入交接包；缺少临时目录不代表源码丢失。

### macOS 跨设备接收与正式启动闭环

- 输入包为 `XS-完整交接包-20260825-061835.tar.gz`，SHA-256 为 `686485585d8fc5df30face62aee0a3bf14e23528e47b9e1e000b78da2c68d7d2`；同步前桌面项目保留在 `/Users/example/Desktop/XS-同步前备份-20260825-105143`。
- 修复冷设备工作区顺序：先按拓扑预构建上游导出，再运行包测试；测试后重新 build，最后执行 typecheck/根门禁。这样既避免下游测试在无 `lib` 冷机上解析失败，也避免 artifact 测试清理 `lib` 后根工程读取不到 `@xiaoshe/memory`。
- 本次 V5 交接冷安装又修复了 pnpm 路径型 `--filter` 的 cwd 边界：macOS/Windows 的接收与全量验证脚本现在都会显式进入解压后的 XS，再运行 14 个 workspace 包；从 Finder、交接工具目录或任意外部 cwd 启动不再静默匹配 0 包。`tests/handoff-cold-install-order.test.ts` 已覆盖四个脚本。
- 修复 macOS `/var` 与 `/private/var` 的真实路径边界、无 shell pnpm 入口解析，以及两个真实 Profile 并行改写共享构建产物的竞态。
- 正式 web Profile 现在同时安装桌面控制、完整 Product Bundle 及八个直接 Product 依赖；不再出现 `runtime-routes` 等待 `xiaosheMemory` 或子包无法解析。
- `~/.zshrc` 的 `ss` 入口会先移除旧 alias 再声明函数，避免 zsh 在 `ss()` 处解析失败。
- 正式服务当前由 launchd 管理，根目录为 `/Users/example/Desktop/XS/runtime/DSH`；主页、Heartbeat、Memory、插件事务和品牌端点均已返回 HTTP 200，桌面桥状态为 `ready`。

## 最终验证证据

- 2026-08-26 macOS 当前最终门禁：18 个 workspace package 共 186/186 项测试通过，全部已声明 build/typecheck 通过；根 Vitest 32 个文件通过、1 个条件跳过，106 项通过、5 项平台条件跳过；Python Bridge 15/15、根 typecheck/build 通过。
- 正式 adapted 浏览器复验：历史 2269×1214、1440×900、1024×768、740×900、390×844 加本轮 412×820 设置页均通过；唯一正式 favicon、左右收放、命令面板、状态/记忆/能力、窄屏 overlay、零页面级溢出和零 console error 均成立。
- 2026-08-27 长会话导航增量：正式 `3080` 的 3 个用户轮次生成 3 个独立短线标记；悬停预览、点击定位、离底阈值回底及按钮自动消失均通过。390/520/760/1024/1440 五档与亮/暗主题无横向溢出，控制台为空。
- 2026-08-27 展示字体增量：顶部会话标题与中央“小蛇”字标改用跨平台中文展示衬线栈，字重为 `600 / 650`；正文、工具文字、布局、流光和正式 Logo 不变。适配壳 41/41、typecheck/build 及 2269/1440/1024/740/390 五档正式浏览器回归通过，控制台 `0 error`。
- 2026-08-27 全页自适应增量：1180 紧凑桌面、2560 超宽屏、1920/1280 桌面、740/390/375 移动端及亮/暗设置全部通过；设置层级、跨断点存活、40px 触控目标、窄屏去重和超宽输入器合同均成立。随后 2269/1440/1024/740/390 完整交互回归再次通过，控制台 `0 error`。
- 2026-08-27 Windows 当前完整 Node 24 `pnpm.cmd run check`：PASS。根 Vitest 33 个文件通过、1 个条件跳过，111 项通过、1 项平台条件跳过；Python Bridge 15/15、根 typecheck/build 通过。V6、插件治理、正式原生壳和可重定位 Product Profile 均通过。
- `pnpm.cmd --filter "./packages/**" -r --if-present test`：16 个 workspace package、合计 163/163 项测试通过。
- 16 个 workspace package 的全部已声明 typecheck/build：通过。
- Phase 7 正式离线工件复验：10 包、零源路径泄漏、10 个本地 override、7 个 HTTP 200、sentinel 未变化。
- 真实模型审批回放：DeepSeek V4 Pro 回合完成，重连前后两个审批 ID 均稳定，持久 asked/decided 与精确文件输出通过。
- 真实 compaction/完成凭证：DeepSeek V4 Pro 摘要、完整持久事务、surface replacement、verified receipt 与零未验证项全部通过。
- macOS 真实动作：真实截图、唯一 AX 元素、局部新鲜度、CGEvent 点击、动作后截图、AppKit 业务见证和关闭动作 fail-closed 全部通过。
- macOS 双屏安全：2 块真实屏幕、副屏元素过滤、不可见元素 stale、越界坐标拒绝和无副屏动作全部通过。
- 历史 V6 Profile verifier：baseline/root/client/brand/heartbeat 全部 PASS；当前 adapted 壳停用、Product 服务保持、sentinel 未变。
- 历史 V6 真实浏览器：七档视口无横向溢出，桌面 232/292、双 48px 折叠、双 24×24 折叠键、720px 输入器、36px 发送键、亮/暗主题、左右 overlay、系统页和插件 modal 均通过；最终导航控制台 0 error / 0 warning。该结果只用于回归比较。
- XS 根工作树与 DSH 的 `git diff --check` 通过。`runtime/xiaoshe-legacy` 的全量检查仍会把交接前已有的 Windows CRLF JSON 报为 trailing whitespace；本轮未修改这些历史文件，也未擅自规范换行、文件模式或 symlink。
- 最新交接 manifest 必须在全部源码、证据与说明落盘后由创建交接包脚本重新生成并反向验证；精确文件数和字节数以 manifest 的 `summary` 为准。

关键最终 SHA-256：

- 正式 adapted Client 源码：`752BF0A3D6AE3C1A61FB4B35B18080604F2038B8662B8B91C03685872D8590EB`
- 正式 adapted CSS：`55ABC1F6A09B82468E27BB9651F5D0B9430B0F9220D5FF07A6594687B64C43BF`
- 正式 adapted Client 构建产物：`BE3D2E55B7BBAE771FE49523BF9CDD384AB40A66866346A09269C90814EB9A57`
- 正式 adapted 响应式状态测试：`97C5D8732B270C2DF53F2BE96F4393D696BB7ECD299E6929B0B564915CC4678A`
- 正式 adapted 浏览器视觉验证器：`C65957D0D1F223B64DD591D33D8E47CF64AD41E3DAB56BA6A968CE9B2F280CD5`
- 正式 adapted 描边源：`AC2B7C8F62F571C62A993C23D2FF115546C9B7B52A1BD3C01E7F13DAEA448B8A`
- 正式 adapted Product patch：`5EE7FB40318F741EA40C7809344D30CE55A9BD22165105A0CE975DB6CEA6CC9C`
- 老版母版与冻结副本整树：`2EA18A7CB27DD5FB5F407A658DE75E5751DEC28F6FAE17F51979E05B9645E17F`
- 候选 V6 Client 源码：`97F5FD1A479DEAAA85E3580874694DE621F9A5436F1F6FDFB94A54C6F3359F0A`
- 候选 V6 Heritage CSS：`40907D532DB853253EF12FA68CE1C145EAA4CA1C0F7018428B9B111E175809A2`
- 候选 V6 Client 构建产物：`3A16F50504E5BE478BF05060B7964C67937C4B43204A986D268B8B6C880C05C5`
- 候选 V6 覆盖 patch：`BE863B50F1E8AB48881D1938F9D78E43CB55E28D74BEEE68B412E81DFA5E10B2`
- 候选 V6 Profile verifier：`4F8B100CDA2069CE941A7FF0D1422D25DB5C482657292B4D33DDDBC942773134`
- Heartbeat 启动竞态修复：`DD300018CDF50A91F24082BE5E954B0CD954587C0548303B0C6E7E83DB9DCE97`
- 正式品牌资源（V6 与旧版逐字节一致）：`3A919A69C3B6F425545957AACACCD0199364A233BB3A0F4DB827F3B5D1EFDD75`
- 候选 V5 Client 源码：`1DEC0AE946691981C5F8FE00B883EE8DD9EB08BDA21EFB7F9175F7C65431F6BE`
- 候选 V5 Client 构建产物与在线脚本：`6357BC28814573481345D8688FB2E9EDEDD7FB5CE3E44DC659654D4B332EE559`
- 候选 V5 浏览器生命周期测试：`CF62577FF173DA23B1CC5FE1F68F6E112EDE6230792A4EE70ADE2E86AAFB5989`
- 候选 V5 正式品牌哈希测试：`0E4A588172CE000778CCBD865168D25EEAE931AE4A01885F3972D67EADACA2B9`
- 候选 V5 运行时视觉合同测试：`313C917A2FD4E185813B8CAC3EE0A2F2A2E022D684AE8C7C2F9C3B37D864A858`
- 候选正式品牌资源：`3A919A69C3B6F425545957AACACCD0199364A233BB3A0F4DB827F3B5D1EFDD75`
- 候选覆盖 patch：`A4B5282B1085284849CD06A3014F0DA18893A51F378E4AE54E59FC0EFFC3B5D0`
- 候选 Profile verifier：`CB6FC80E19A78A9EDA4A395B9FD57CCA53813863D1D6408C92B01988E67DC0E4`
- 候选 V5 结构化验收证据：`B9D1A3669815B9EA534AEF1C14E21B59E6E530CF3689A95876E46CE38D44B135`
- Heartbeat writer-lock 修复：`9DDA26E0A36CE18275D7EA7B95B972D4547D64CBA182CB204217003DB53F167A`

## 保留的本机证据与测试残留

这些目录不进入正式交接载荷，也不是正式 Profile。安全策略拒绝了递归清理，因此保留并如实登记：

- Phase 6：
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-phase6-browser-8577eb70b0d4432693424ae3a86c245a`
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-phase6-pack-probe`
- 界面候选：
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-ui-candidate-proof-2901cc440454460ab9f4aaff9d452729`
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-ui-candidate-final-7b2b63452da44e7a8a7e687fc3755017`
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-ui-candidate-acceptance-b7e28e35928041aca80e903195d482c4`
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-ui-candidate-final2-32b47367f4f641ffb80bf04b2323181c`（只用于诊断 writer-lock；不可作为最终证据）
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-ui-candidate-lockfix-c6200fecc5604376ba5506124f3e16b3`（最终 PASS 证据）
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-candidate-v6-proof-20260826-1`（V6 Profile 修复前后对照）
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-candidate-v6-proof-20260826-2`（当前 V6 最终 PASS 与 `58818` 查看现场）
  - `C:\Users\example\AppData\Local\Temp\xiaoshe-heartbeat-debug-20260826-1`（Heartbeat 启动竞态诊断基线）

第一次浏览器验收点击“新会话”还揭示了一个现有边界：DSH 会话目录不随隔离 `DSH_HOME` 隔离。它在全局会话根留下了一个已确认为空的目录，清理命令被安全策略拒绝：

- `C:\Users\example\.xiaoshe\conversations\session-0dc28a00-0388-4d30-880a-2f86e660e60e`

这不是数据泄露或正在运行的会话，但后续应单独设计可测试的 conversation-root 注入，不应在本轮顺手改 DSH 内核。

## 仍为 release-held，禁止误报

- 没有用户指定并逐次确认的真实第三方插件；本轮只使用仓库内受控 fixture。未来每个候选都必须重新审计和确认。
- Host Bundle 没有 OS 沙箱；确认记录不等于隔离执行。
- Phase 7 Product 闭包已经可重定位；DSH Harness 本体和 Node/pnpm 运行时仍由锁定源码与交接安装器提供，不应把 Product 工件误称为整机免安装包。
- macOS 真实屏幕动作、辅助功能链和当前双屏 fail-closed 已通过；跨屏动作 Provider、Windows、Linux、125%/150% DPI 与其他显示器拓扑仍待对应环境复验。
- 正式 adapted 壳已完成当前 Windows 本机的多档浏览器工程验收，但仍需用户在目标显示器做最终主观视觉确认；尚未发布、合并或创建提交。
- 工作现场侧舱 V1 本地工程已完成并进入离线 Product 工件；浏览器手动交互、CSP/XFO 降级和目标显示器视觉尚未验收，禁止把这些 `release-held` 项误报为完成。
- API Key、个人 DSH 会话、`.state`、日志和系统权限不在交接包中；新设备必须重新配置。

## 当前接力起点

1. 解压后先运行对应平台的 `验证交接`，再运行 `接收并安装`；验证通过前不要 reset、clean、切分支或只用基准提交覆盖现场。
2. 阅读 `交接工具/从这里开始.md`、本文件、`docs/evidence/work-surface-dock-v1/acceptance.md` 和 `docs/evidence/native-shell-legacy-adapted/acceptance.md`，按其中命令在新设备重建正式 adapted 隔离 Profile。
3. 若继续当前视觉工作，只修改 `@xiaoshe/native-shell-legacy-adapted` 的工作 UI；`runtime/xiaoshe-legacy/ui` 与其 `ui-baseline/` 禁止修改，V5、V6 和原 `@xiaoshe/native-shell` 保持可回退。品牌只使用老版正式资源；不要重新启用整套 `ui-conversation` 来借用一个设置行。
4. Phase 4–8 本机实现、真实模型回合、审批回放、真实 compaction/完成凭证、adapted 工程实现、终端共享运行时、工作现场 V1、长会话消息导航与 macOS 受控真实动作/双屏拒绝边界已经完成，不要重做；可按已有 evidence 复验。
5. 工作现场下一步只做用户在场的真实浏览器交互、CSP/XFO 降级和目标显示器视觉确认；V1 不包含“小蛇接管操作”。
6. 并行未决验证仍是正式 adapted 壳的目标显示器主观检查，以及对应设备上的 Windows/Linux 浏览器、DPI、跨屏动作与其他显示器拓扑；未有真实证据前继续标为 `release-held`。
7. 任何发布、合并或创建提交都必须另行确认。
