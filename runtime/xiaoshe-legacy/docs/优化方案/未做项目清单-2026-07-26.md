# 未做项目清单（2026-07-26 整理）

> 来源：`docs/优化方案/`（D1-D5）+ `docs/优化方案/小蛇视觉升级方案-2026-07-24-视觉特化版.md` + 施工进度表「未做/后续」行。
> 整理口径：只列**明确未做**的；已完成、已承认残余、已暂缓观察的不重复列入。

---

## 一、安全/沙箱类（价值最高，堵已知硬伤）

| 编号 | 方案来源 | 项目 | 内容 | 价值 | 工作量 | 前置依赖 |
|---|---|---|---|---|---|---|
| S1 | D1-1b | **出站白名单代理** | 工具子进程（run_command/jobs）出网默认经受控白名单代理 + 环境擦除；模型 curl 与工具出网物理分离 | 堵「run_command 等于打开联网出口」已知硬伤，注入成功也带不出数据 | 中（2-4天） | 无 |
| S2 | D3-3c | **Docker 沙箱化执行** | `DockerExecutor` 让破坏性 shell 命令在容器里跑，无 Docker 时优雅降级本地并显式标注「未隔离」 | eval 敢跑 `rm -rf` 类破坏性任务；与 A2b AppContainer 互补（Win 原生 vs 容器） | 中（2-4天） | 需拍板是否允许 Docker |
| S3 | 视觉 §5.4 | **Mac seatbelt 沙箱** | Mac 端 sandbox-exec 文件系统+网络 profile，对齐 Windows AppContainer | 双平台沙箱闭环；当前 Mac 无沙箱 | 中（2-4天） | 回 Mac 做 |
| S4 | 视觉 §5.1 | **统一信任标签层** | 污点从内容匹配升级为来源/能力标签（FIDES/CaMeL），补齐 denylist 非完备短板 | 最大杠杆点：内容检测漏掉的 payload 靠来源标记识别 | 中（2-4天） | 无 |
| S5 | 视觉 §5.2 | **StruQ/Spotlighting 可落地子集** | 通道分离契约（tool role 声明）+ 统一标记（随机分隔符/datamarking）+ 层级声明（system 明文 tool 内容非指令） | 正交补强污点门，零训练成本 | 小（≤1天） | 需回归套件验证 Kimi 有效性 |

---

## 二、能力/循环类（提升长任务可靠性）

| 编号 | 方案来源 | 项目 | 内容 | 价值 | 工作量 | 前置依赖 |
|---|---|---|---|---|---|---|
| C1 | D5-5a | **ReAct 显式轨迹** | 工具循环先产简短 thought/plan 再调工具；thought 记入 history/日志/压缩保留策略 | 长任务可诊断；压缩保留关键决策；是 D5 地基 | 小（≤1天） | 无 |
| C2 | D5-5d | **ADaPT 按需分解** | 主循环检测到「卡住/无进展」时自动递归拆子任务给 spawn_subagent（当前只有停滞提醒文案，无真分解逻辑） | 长任务破局，不预先过度规划 | 小（≤1天） | C1 先做（共用基座 system） |
| C3 | 视觉 §4.1.2 | **时间轴金字塔式视觉降级** | 新图完整保留，越旧越降清乃至文本化（caption/OCR 摘要）；保住缓存前缀 | 视觉 token 成本大头；稳 75% compaction 触发线 | 中（2-4天） | 需 A/B 校准阈值 |
| C4 | 视觉 §4.1.3 | **PDF 两阶段渲染 + 按页 blob 检索回捞** | 缩略整页定结构，目标元素按原始分辨率裁剪识别；长文档先检索证据页只精读相关页 | PDF 处理成本与精度双升 | 中（2-4天） | 无 |

---

## 三、评测/验证类（已部分完成，剩尾巴）

| 编号 | 方案来源 | 项目 | 内容 | 价值 | 工作量 | 前置依赖 |
|---|---|---|---|---|---|---|
| E1 | 进度表 | **friction.py 消费压缩事件** | P2-7 压缩事件格式已就位，friction.py 读取统计（目前只能标 `compaction_observable=false`） | D3 摩擦报告完整化 | 小（≤1天） | 无 |
| E2 | 进度表 | **换机手册 v13** | 下次交接前出，含本批审查修复内容 | 交接完整性 | 小（≤1天） | 无 |

> ~~E3 Mac OCR 置信度门控真机验收~~ **✅ 已完成（2026-07-27，回 Mac 真跑 Vision）**：高置信单跑放行
> （清晰大字 confidence 1.0，look/dual 调用 2→1）、低置信触发补跑（小字 0.5<门，调用=2）且识别不降、
> WinRT 无信号回落现状不动。tests/test_ocr_confidence_gate.py +2 真机条（共 20），全量 2061 绿。
> 详见 CONTRACT.md「OCR 健壮性增强」§5。

---

## 四、已承认残余 / 暂缓观察（不施工，仅记录）

| 编号 | 内容 | 原因 |
|---|---|---|
| R1 | 注入缝隙两条：短祈使句/base64 绕扫描 | denylist 型检测固有局限，治本靠白名单化/隔离 |
| R2 | 视觉方案观察档 §4.1.2/§4.1.3/§5.1 | 已列入暂缓，非紧急 |
| R3 | 超时逃生盲区（agent.py:1261-1265） | 输入层重构风险高，审查记录不改 |
| R4 | 96a8593 断裂提交 | 历史流程问题，无法改代码，记录 |

---

## 五、建议施工顺序

**第一梯队（安全硬伤 + 地基）**：
1. **S1 出站白名单代理** — 堵最大安全缺口
2. **C1 ReAct 显式轨迹** — D5 地基，后续 C2 依赖它
3. **S5 StruQ/Spotlighting 子集** — 零成本正交补强

**第二梯队（能力扩展）**：
4. **C2 ADaPT 按需分解** — 长任务破局
5. **S4 统一信任标签层** — 安全架构升级
6. **E1 friction.py 消费压缩事件** — 小尾巴收净

**第三梯队（平台/成本）**：
7. **S2 Docker 沙箱** — 需拍板 Docker
8. **C3 时间轴视觉降级** — 需 A/B 校准
9. **C4 PDF 两阶段渲染** — 按需
10. **S3 Mac 侧沙箱** — 回 Mac 做（E3 已于 2026-07-27 完成，见三节标注）

---

## 六、要你拍板的取舍

1. **S2 Docker**：是否允许引入 Docker/bubblewrap？（默认 local 不引依赖，容器是显式 opt-in）
2. **C3/C4 视觉降级/PDF**：当前视觉成本是否已构成瓶颈？（若否可继续暂缓）
3. **S3/E3 Mac 侧**：是否等回 Mac 再做，还是当前 Windows 侧先设计好？
