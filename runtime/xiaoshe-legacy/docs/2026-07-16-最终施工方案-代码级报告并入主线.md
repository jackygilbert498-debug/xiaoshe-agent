# 最终施工方案 · 壳对比代码级报告 12 条并入 A1-A12 主线

> 日期：2026-07-16（对抗审查修订版）
> 两个输入源：①《壳对比·代码级对比报告》（另一台设备产出，5 壳 21-agent 深读，给小蛇 12 条分级建议）②ke/docs 既有优化方案全家（A1-A12 施工定稿、70 条代码审查清单、壳对比可学点、六维前沿调研、视觉三件套、记忆/自扩展/SSRF 方案）。
> 本文是两源合并后的**唯一推进依据**：近期施工批次（批0-批3）+ 战略主线（三地基→中层→外围）+ 冲突裁决记录 + 不做清单。
> 核实纪律：报告 12 条建议的「现状」声明已由 5 个核查 agent 逐条回源码验证，**12/12 全部 CONFIRMED**（无一已修、无一行号失实），且发现两处比报告更严重：建议⑧ MCP 协议级 error 也会被永久软屏蔽（不止超时）；建议⑨ 撞名后缀可使工具名超到 73+ 字符。
> 本文初稿又过一轮三向对抗审查（完整性 vs 报告 / 一致性 vs 既有方案 / 可行性与事实），2 HIGH + 4 MED + 8 LOW 已全部织入下文。

---

## 施工进度（2026-07-25 对账 · 1909 测试全绿 · Mac/Win 双平台金标准过 · SSRF/OCR 双拍板项已落地）

> 从换机基线 `2255a70` 起共 90+ 提交完成，全程 TDD + 每单元红队/对抗审查 + 关键项真 Kimi 实测 + 真机金标准验收。

| 部分 | 状态 | 备注 |
|---|---|---|
| 批 0（H1/H2/F03 止血） | ✅ 完成 | 对抗审查 2H+2M 修净 |
| 批 1（审批指纹/打断保留/compaction 强化） | ✅ 完成 | 两轮红队 |
| 批 2（边界包裹/双时钟/MCP健壮/permission对齐） | ✅ 完成 | 分 2p1/2p2/2p3；**尾欠已清**（见下「批2尾欠」行） |
| 批2尾欠（batch2p3） | ✅ 完成 | MCP 失败重连宽限（启动配置留存→重启重发恰一次→再失败才软屏蔽，协议级 error 同路径，防重连风暴/进程泄漏/并发双重启）+ `_read_bracketed` SSH 丢结束符 2s 超时逃生（msvcrt/select 平台非阻塞探测，注入化判定零真 sleep）；nextCursor 分页前批已落；红队真杀进程复现 + 固化旧行为测试按方案一并改 |
| 批 3（架构守卫/出网审计/契约补强） | ✅ 完成 | |
| **基M1 记忆 v2 迁移** | ✅ 完成 | 增量1双读 + 增量2写source+渲染 + 增量3 DELETE合并；真 memory.json 已迁 |
| **基M2 PTC 受限解释器** | ✅ 完成 | 骨架+接真执行+收尾+字符串助手；**两轮红队零发现** + 真 Kimi 省钱实测 |
| **基M3 steering 单一归属** | ✅ 完成 | 路由+接真repl+红队HIGH/MED修净（开关 gated 默认关） |
| A4 edit 手术刀 | ✅ 完成 | 红队安全全顶住 |
| A5 glob/grep | ✅ 完成 | grep 不泄漏敏感（专项锁死）；输出路径归一化正斜杠（Win 真机补漏） |
| A2a 技能库 SKILL.md | ✅ 完成 | 真 Kimi 自扩展实测；**后台自学已落**（见下行） |
| A2a 后台自学 | ✅ 完成 | SessionEnd 复盘成功经验→子 agent 产 pending 技能（`.state/skills/pending/` 目录隔离=字节冻结物理保证）→ `:skills`/`run.py skills` 人审硬门激活（approve 重走全套净化管线）；省钱闸（轮数<2/摘要<300 不烧 LM）；注入面三道防线照齐；红队 16 探针 15 过（1 条宽容解析判定非漏洞记录） |
| ReasoningBank（经验层） | ✅ 完成 | episodic 三字段坑/因/改 |
| effects.jsonl 副作用账本 | ✅ 完成 | 挂 _run_tool 覆盖主循环+PTC；`:effects` 命令查看会话改动 |
| compaction 反抖动 | ✅ 完成 | 壳三连之一 |
| compaction 75%触发点 + 窗口自校准 | ✅ 完成 | 真探针取超限报错形状→预算=真窗口262144×0.75（两道闸统调）；calibrate.py 从 400 报错解析权威真窗口落盘；agent._send 溢出重试网（缩史重试、非砖死） |
| A6 hooks（Pre/PostToolUse 闸+自动化） | ✅ 完成 | 命根子 hooks.json 模型读写不了；红队 HIGH/MED/LOW 修净 |
| A7 持久放行（跨会话） | ✅ 完成 | 绑整条命令指纹；裸名高危不持久、含污点不落盘 |
| A6增量2 会话生命周期 hook | ✅ 完成 | SessionStart/SessionEnd；红队逮修 1H(通配绕过命根子)+2M(阻塞谎称/收尾顺序)+1L |
| Cheatsheet 战术小抄（经验层最轻一档） | ✅ 完成 | note_tip 存成功战术、DC-Cumulative 注入、三级阶梯打通；真 Kimi 端到端；红队逮修 MINJA 洗白 MED+读路径 LOW |
| **A2b 可执行工具全链** | ✅ 完成 | sandbox.py（AppContainer+Job 真隔离）→ run_sandboxed → user_tools 注册表+哈希清单 → propose_tool+REPL 人审门（Path B.1-B.4）；红队真逃逸逮修 3 条 + 审查 7 确认全修 |
| **视觉 SoM 轴** | ✅ 完成 | imaging.py 纯标准库 PNG/画框/标号 → observe(mark=true) SoM（真 Kimi E2E）；红队两轮（恶意 PNG/Retina 脱靶/region=None） |
| 视觉链补强 | ✅ 完成 | screenshot 整屏截图工具 + OCR 词框(boxes) + click_at——「截屏→ocr→click_at」链闭合；PDF 读取 Win 改 WinRT；render 布局审计 + dHash 视觉回归基线 |
| 省钱工程 | ✅ 完成 | NOTES.md 跨压缩存活工作笔记 + tool result clearing（旧大工具结果缩占位）；审查 8 确认全修 |
| 文件级 undo | ✅ 完成 | 写前快照 + `:undo` 一键还原（接 effects 账本）；审查 8 确认全修 |
| A3 记忆大脑 | ✅ 完成 | 分区归类 + `:memory` 查看/剪枝 + 会话启动速览；审查 5 确认全修 |
| **统一「裁剪-重问」子系统（视口注册表+zoom/pick，工具 35→38）** | ✅ 完成 | spec 四期全落地（P1 坐标变换心脏→P2 look→P3 zoom→P4 pick），Mac 全适配（Vision OCR/CGEvent/NSScreen 预检）+ OCR 反色补跑；五轮红队修 9 条；**Mac 双金标准过 + 真 Kimi 无头 E2E 过（8 轮自驱）；Windows 双金标准双轮过**（`evals/gold_standard_win.py`，证据 `docs/验收/裁剪重问-Win金标准-证据.md`）。平台发现：WinRT OCR 系统性丢弃计算器孤立数字字形（裁剪放大疗法对 Win 此目标无效），由 UIA 框源承接 |
| 视觉链加固（吸收视觉升级方案 §4.2.3/§4.3） | ✅ 完成 | 术前拦截（纯 uia 源编号消失拦点击，fail-soft）+ 像素差分读回（`imaging.diff_ratio`，补 click down/up 观测残余——AX diff 与像素皆无变化判疑似未生效不装成功）+ zoom ≤3 级深度闸（视口链深，CONTRACT 入档）+ pick 出口偏移提示（编号位置信号）+ 免 zoom/倍率回退 2/3/1 引导 + focus 抢焦点 3 次上限请用户接管；红队逮修恶意 label 换行伪造点击成功行 |
| OCR 换引擎决策包（视觉升级方案 §4.4.2） | ✅ 完成 | 决策文档 `docs/优化方案/OCR换引擎决策包-2026-07-24.md` + 盲区回归集 `evals/ocr_blindspot_suite.py`（孤立数字/繁简混排/zoom 小字三类，HTML 源入 git 图不落盘）。**首次真机量化：门禁用例合并召回 73.9% < 85% 门槛=不达标**（孤立数字 100% 丢弃复现；塌方在 zoom 小字 12px 召回 25-31%，UIA 与放大都接不住）→ 按门槛走**换型验证立项（待甲方批）**：Tesseract 臂双跑 ≥85% 才切换，不批退 Kimi 直读双跑 |
| 视觉升级方案第二优先（§4.5.1/§4.5.2/§4.6.3/§4.1.1/§4.4.3/§4.6.2/§2.3.1） | ✅ 完成 | JS 审计扩对齐/拥挤/遮挡三类（真机零误报校准，蒙层/弹窗豁免）+ 区域化 dHash 比对（render_check 报变化区域行列）+ FitLoop 2 轮预算退化回滚 + render_check 三值判决（INCONCLUSIVE 不当 PASS）+ 分辨率三档（OCR 词密度选档，阈值集中留 A/B 口）+ Mac OCR 置信度门控补跑（高置信单跑放行，WinRT 无信号严格回落现状；Mac 真机待验）+ 分层评测矩阵 `evals/matrix.py`（定位/执行/完成×双平台，编排金标准+盲区回归集，日志归因到层）+ 循环边界确定性提醒层（剩 3 轮恰一条，与 5c 停滞检测正交）。金标准回归两次因前台锁（用户会话占前台）中止——非代码问题，闲时补跑 |
| SSRF 架构层（拍板 1a：代理侧 REJECT + Python 预检） | ✅ 完成 | 治本侧：Clash 内网网段 REJECT 规则片段交付（`docs/优化方案/SSRF-Clash内网REJECT规则.md`，含贴法+验证脚本，**待用户贴进 Clash 配置**）；纵深侧：`web.py` fetch 层 DNS 静态解析预检（解析落内网即拒——IPv6 映射/混合 A 记录/尾点域名全覆，fail-open 如实落 network.log 审计；只挡静态指向，rebinding 由代理侧治本）；红队六项全过 |
| OCR 换型验证（拍板 2a：Tesseract 臂实测） | ✅ 完成（结论：不切换） | Tesseract v5.4.0 已装（chi_sim/chi_tra 落 `.state/tessdata/`），回归集双臂双跑（psm 探针定 6）：门禁 Tesseract **63.0% < 85% 不达标，主链路不切换**；失败模式正交（数字 100%/小字 65.6% 强、CJK 63.3% 弱），「WinRT 主+Tesseract 小字辅」纸面 84.8% 留档复审候选；决策包已回填 §5，退**排序 2 Kimi 直读双跑**备案 |
| OCR 排序 2：Kimi VLM 直读双跑（zoom 小字兜底） | ✅ 完成 | `_vlm_fallback_read`：闸控触发（词数/密度/**垃圾词率**三闸+预算 6 次/视口 1 次）→无头双跑→Jaccard≥0.6 行集一致→污点包裹+标注「未经本地词框确认、不可 pick」；真机实测 zoom 小字 token 可用度 **31.2%→81.2%**（一致行逐字核读全对，分歧行精确剔除）；二轮校准补**闸③垃圾词率信号**（单字符非 CJK 碎屑+罕见字符指纹 >25% 触发，CJK 单字豁免）——「词多全误读」形态从不触发到正确触发（34.3%）、健康 CJK 零误报；决策包 §7 已闭环。至此 OCR 短板全链路闭环：CJK 主场景 WinRT 96.7%、孤立数字 UIA 承接、zoom 小字 VLM 兜底 |
| D3 真实任务 eval（图纸驱动→真实驱动转型） | ✅ 完成 | 按用户 5 类日常任务（看图写 xlsx/双文件夹特征匹配改名/收集媒体/视频裁剪截图/处理小脚本）建 `evals/real_tasks/` 评测基建（合成 fixtures+确定性 verifier+摩擦报告），真 Kimi 首跑 2/5 过——产出**问题清单**（`docs/验收/D3真实任务-问题清单.md`）：P0 write_file ADS 护栏误伤/P0 .state workdir 全灭/P1 批量 read_image 绑定弱/P1 GBK 乱码/P2×3 |
| 记忆/经验层炼化（§3.1+§3.2+D9） | ✅ 完成 | 记忆第四操作 superseded 软失效（防环+链长上限+`:memory revive`，注入只给 injectable）+ Cheatsheet ACE 条目化（note_tip update 编号改写，旧行式双读迁移）+ 后台 LM 统一预算闸门（日 20/会话 3，超限落信号版 delta）；红队逮修 2 条（超长条目注入膨胀/负数账本绕日帽） |
| FTS5 统一检索层 | ✅ 完成 | `harness/fts.py`：unicode61+自做 bigram（弃 trigram——两字中文词静默不命中），记忆/小抄/episodic/会话四类统一索引（滤 superseded），`memory.search`/`search_sessions` 两消费点，写路径钩子+启动校验重建+扫描降级如实标 degraded |
| A2a 自学第二级 | ✅ 完成 | 失败轨迹配对（坑+爬坑优先进 prompt）+攒批触发（够 3 条/1500 字符烧一次）+沙箱重放门（重放≠激活，结果上人审卡片）+编译晋升（小抄奏效≥3 次自动提名 pending 技能，文案→真机制） |
| 账本加固（§6.1-6.3） | ✅ 完成 | irreversible 判定+三态口径（可撤/未快照不可撤/本质不可逆，`:effects` ⛔ 标记）+选择性快照（敏感/超大/二进制不进快照，纵深复验防 .env 进 undo 目录）+undo 墙钟（中止文件不动栈保留，如实报部分态） |
| §5.5 注入回归套件 | ✅ 完成 | 40 用例 9 类表驱动+状态断言（拒后文件零变化）+组合攻击双链路+7 变异红→绿验证；锁定 3 缝隙（save_skill 污点裸奔 MED 已修转正/短祈使句/base64 已承认残余） |
| D3 修复批 + save_skill 污点修复 | ✅ 完成 | P0-1 write_file 内容参数豁免路径扫描（content/text 键不扫路径形态）+P0-2 headless workdir 在 .state 显式告警+P1-4 run_command GBK 解码回退链+P2-5 无头拒绝话术如实+save_skill 进 `_fact_from_untrusted` 污点拒存（确定性拒强于升 ask） |
| 传输统一 curl | ✅ 完成 | `curl_transport` 共用件：web_fetch 代理从 argv 挪 stdin 配置（凭据不进进程列表，含换行硬拒防配置注入），kimi_client 同转义收敛；出网裸传输站岗守卫；SSRF 护栏一行未动 |
| P1-3 批量 read_image 图-名强锚定 | ✅ 完成 | vision wire 图文交错——每张图紧邻自己的文件名标签（`_sanitize_label` 净化防伪造多行，超 VISION_LIVE_MAX 截断如实点名给 recall 出路）+read_image 多图引导与图序自检；离线验收件（T2/T3 错序回放）改前挂改后过；**真 Kimi 复跑 T2/T3 双双翻绿**（T3 从 14 轮降到 6 轮）；四道回归门（单图/zoom SoM/真机金标准/全量）零回归 |
| kimi_client 连接阶段重试 | ✅ 完成 | 非流式 `_post` 对 exit 6/7/35（连接失败=请求未发出，天然幂等）最多 2 次递增重试（cap 8s、可观测）；56/28/硬超时（请求已发出下落不明）绝不重试；headless 实链 stderr 实锤生效——D3 复跑被代理 TLS 抖动掐死的问题闭环 |
| 小尾巴收净 | ✅ 完成 | GBK 解码回退链挪 `_io.decode_cmd_output` 共用（jobs 日志/schedule 监工输出接入）+ 无头两处边角拒绝话术如实（taint_gate 拦/hooks 收紧被拒时报「审批策略拒绝（无头）」，交互模式一字不变） |
| 清零批（2026-07-25 晚） | ✅ 完成 | **安全**：Win32 尾点/空格等价绕过 .state 敏感护栏修复（`_win32_equiv` 归一 + 字面容∨归一容**并集**判定——归一只加拦不放宽；打捞自并行会话 worktree 未提交修复并补完并集语义）+ worktree/分支清理（3 个已合并 claude 分支+2 worktree 移除，docs/progress-page-v07 保留） · **P2-7**：压缩事件落 system 级 JSONL（四触发点带压前后规模/原因/清理条数） · permission `is_headless()` 公开访问器 · **文档欠账**：CONTRACT 补 10 单元契约段 + D1-D11 决策清单回填（D10 标部分） · **P2-6**：ffmpeg GOP 精确裁切经验入小抄 |
| T5 修后复跑 + 平台排序差异修复 | ✅ 完成 | 代理恢复后复跑 T5：`.d3/20260725-225427/` 1/1 过（9 轮）。修复 Windows `Path` 排序大小写不敏感导致的 verifier 期望错位：`expected_t5_names()` 改为大小写不敏感字典序，prompt 同步明确规则，确保 agent 与 verifier 跨平台一致。D3 问题清单 5/5 全绿 |
| **24号后提交审查修复批** | ✅ 完成 | 对 7/24 后 39 个提交逐组审查（12 组并行），发现 2 HIGH + 17 MED + 若干 LOW。已修：SSRF Clash 规则去掉 `no-resolve`（原规则对域名请求直接跳过，堵不住 rebinding）+ save_skill 污点拒存补 name 字段 + 第三跑空文本词不过滤替换 + 多显示器负坐标像素读回不钳到 0 + tesseract 调用超时/OSError 收敛 + :undo 不可逆警告分级（删除/破坏/外部请求才弹）+ SoM 编号截图固定中档 1600 + note_tip update 返回原文供核对 + D3 docstring .state 路径修正 + web.py docstring 代理条件化 + CONTRACT 补重连宽限/curl 重试描述/无头话术/Win32 护栏/P2-7 事件格式。记录不改：96a8593 断裂提交（历史流程问题）+ 超时逃生盲区（输入层重构风险高）。 |
| **未做清单收尾批（07-26）** | ✅ 完成 | 按用户拍板（不推荐 Docker/只做 C3 轻量版/白名单默认关/thought 折中/换机手册暂缓/Mac 等回 Mac）完成：S5 StruQ/Spotlighting 子集（tool 返回包「数据非指令」标记+BASE_SYSTEM 通道分离声明）+ S1 出站白名单代理（netguard.py，TOOL_NET_MODE=off 默认关，proxy 模式白名单+环境擦除）+ S4 统一信任标签层（trust.py，来源/能力标签，校准期只记录不拦截）+ C1 ReAct 显式轨迹（BASE_SYSTEM 第⑤条+压缩时 [思考] 标记）+ C2 ADaPT 按需分解（停滞提醒已含拆子任务引导，确认实现完整）+ C3 时间轴视觉降级轻量版（>10 轮旧图降档 768）+ E1 friction.py 消费压缩事件。不做：S2 Docker、C4 PDF、S3/E3 Mac 侧、E2 换机手册（暂缓）。 |
| **未做/后续** | ⏳ | Mac 沙箱+Mac 真机待验清单（回 Mac 做） · 视觉方案观察档（§4.1.2/§4.1.3/§5.1 已部分落地，§4.1.2 时间轴降级轻量版已做，§4.1.3 PDF 两阶段渲染暂缓） · 注入缝隙两条已承认残余（短祈使句/base64 绕扫描，denylist 型局限） · 换机手册 v13（下次交接前出） |

---

## 〇、命名澄清（先排雷，两套 M 编号撞车）

- **基M1 / 基M2 / 基M3** = A1-A12 施工定稿的「三地基」：基M1=记忆 v2 schema 一次协调迁移；基M2=PTC+CaMeL 绑同批；基M3=steering stdin 单一归属。
- **F 编号**（F01-F67）= 70 条代码审查清单的问题项。审查清单自带的 M1-M9 缩号一律改用 F 编号引用（如 审M1→F03 压缩预算、审M2→F24 回合内压缩）。
- **建议①-⑫** = 壳对比代码级报告的 12 条建议。
- 验收测试名一律 `test_` 前缀（unittest 惯例，`测_` 前缀不会被 TestLoader 收集——对抗审查抓的坑）。

---

## 一、近期施工批次（报告驱动 + 70 条推进序，全部 TDD）

### 批 0 · 止血必修（半天内，最高优先）

**H1 · 备份恢复越界覆盖（潜在 RCE）**（F07，70条必修 High）
- `backup.py` 解压围栏钉在 ROOT 而非 `.state`，恶意备份包 `.state/../.env` 可覆盖 .env/harness 源码。
- 修：`_is_within` 围栏根收紧到 `.state`、强制成员落 `.state/` 前缀、`extractall(filter='data')`（顺带消掉 Py3.14 DeprecationWarning）。
- 验收：`test_恢复成员越出state前缀被拒`、`test_点点斜杠穿越成员不落盘`、`test_filter_data生效无弃用告警`。

**H2 · 读密钥命令硬护栏被引号折叠绕过**（F09，70条必修 High + 报告维度③）
- `permission.py` `_cmd_hits` 可被 `.e''nv`/字符构造绕过。修法融合两源：
  - 扫描前**归一化去混淆**：引号拆字还原、sudo/env wrapper 剥离、base64/openssl 解码管道检测（报告维度③经验4 三类变体展开）。
  - **命中混淆原语不硬 deny 而是剥夺白名单强制 ask**（70条定论，防误杀合法 `-join`）。
  - ⚠ **跨文件管道（对抗审查 HIGH）**：强制 ask 在 permission.py 单文件落不了地——真正的放行捷径是 `agent.py:91-95` 的会话白名单 `_approved()` 短路，它在 `permission.check()` **之后**执行，check 返回 ask 也会被白名单击穿。修法：`Decision` 加 `force_ask` 字段（或复用 agent.py:90 taint_gate 同款管道先例），`agent._approved` 对 force_ask **跳过 `_approved_tools` 短路**。
- 诚实边界照录：文本扫描非强隔离，治本在「白名单时不把 _cmd_hits 当唯一屏障」（架构级挂基M2）。
- 验收：`test_引号拆字读env被强制ask`、`test_已always批准run_command后混淆命令仍强制ask`（击穿场景，关键用例）、`test_合法join参数不被误杀`。

**F03 救急一行 · 压缩字符网抬高**（70条推进序②）
- `compaction.py` `DEFAULT_BUDGET_CHARS` 24000→**≈384000**（字符网架空 128k token 预算、成实际触发器白烧摘要+打穿前缀缓存）。
- 取 384000 不取 512000（对抗审查）：512000 纯 ASCII 恰与 128k token 触发线重合，字符网防 base64 骗 token 估算的本职被废（base64 真实 ~2-3 字符/token，512000 字符 base64 可达 ~200k 真 token 直逼 262K 上限）；384000 对应 base64 最坏 ~150k token，留出安全余量。批 1c 统调时复核此值。
- 验收：`test_字符网新值高于token网常规触发但兜住base64场景`。

### 批 1 · 烧钱/丢进度三连（报告 P0，几十行挡真实事故）

**1a · 审批白名单按命令指纹分粒度**（建议①）
- 现状已核实：`agent.py:91-95` 白名单 key 是裸工具名——批准一次 `run_command {ls}` 后本会话任意 `run_command` 静默放行。
- 改法：`run_command` 用「工具名:命令首词」（`run_command:git`），`write_file` 用「工具名:目标路径」，其余维持工具名；**拒绝不入缓存**（CodeWhale 双钥匙）。与批 0 H2 的 force_ask 改造同处 `_approved()`，**两项同批施工减少重复动刀**。
- 与壳-A7「持久放行绑命令模式+cwd」同方向分两步：本批只做**会话内**指纹，持久放行留 A7 阶段，key 格式一次定对预留兼容。
- 验收：`test_批准一次ls不放行del`、`test_同程序换参数不再问`。

**1b · Ctrl+C 打断不整轮回滚**（建议③ + 报告维度②经验5）
- 现状已核实：`agent.py:307-314` `except BaseException` 整表还原，第 14/15 步按 Ctrl+C 全部丢弃；`_finalize_dangling`（agent.py:180）已存在但打断路径没用它。
- 改法：`except` 单列 `KeyboardInterrupt`：本轮已有 ≥1 个完成的工具往返就不回滚，用 `_finalize_dangling` 补齐悬空 tool_calls 正常收尾，合成结果文案明确「用户主动打断，勿自动重试」；一次都没完成才走整表回滚。
- 同域顺带（**纵深防御**，对抗审查修正表述：当前 `_ends_clean` 存档纪律已挡住主场景、Ctrl+C 路径不落盘，此项防的是未来路径变更）：resume 恢复时若历史尾部有未配对 tool_call，原位合成「工具被中断」结果。
- 验收：`test_打断保留已完成工具结果且历史干净收尾`、`test_打断合成结果文案含勿自动重试`、`test_恢复会话尾部悬空toolcall被补配对`。

**1c · compaction 强化包**（建议②+⑤+⑥ + 壳三连 + F03 统调 + **F24 回合内压缩** —— 六源并一包，一次施工）
- **F24 · 回合内节流压缩**（70条推进序④前半，对抗审查 HIGH 补回）：现在压缩只在**回合开头**查一次（agent.py:236），单回合 20 轮工具可膨胀 ~40 万字符→越过 262K→API 400 长任务中途硬失败。修：工具循环内每次发 model_fn 前也查压缩，**节流**（增量 >8000 字符或每 N 轮才真查），防「干长活」核心用例硬失败。
- **熔断**：`maybe_compact` 记连续失败次数，≥3 次停止尝试并 warn，成功清零（cc-haha 曾因此每天烧 25 万次徒劳调用）；`_summarize` 失败截半重试一次，摘要注明「更早 N 条未纳入」（建议②）。**熔断计数落盘 `.state`（跨进程生效）**——报告点名最烧钱场景恰是 schedule 无人值守（每次新进程会话级计数归零则形同虚设），与 hermes 冷却持久化同型（对抗审查补）。
- **保留最早用户原话**：压缩时抽第一条 user 消息截 ~800 字逐字并入摘要头部，不破坏 `pinned_system_end`，二次压缩不叠加（建议⑥，治长任务后段跑偏）。
- **触发点统调**：75% 触发点 + token 预算护尾 + 反抖动（连续 2 次省 <10% 即停）（壳三连）；**字符网与 75% 触发点两道闸一起调**（丙-compaction 定论：只抬一道则另一道先开火使其形同虚设）；复核批 0 的 384000 救急值。
- **窗口自校准 + 超限事后恢复**：真机确认 Kimi 超限报错形状后（**前置：真机探针**），捕获时把 `estimate×0.85` 落盘 `.state/context_limit.json`（与配置取 min），同轮强制压缩重发**恰一次**（跨迭代 attempted 标志防死循环）（建议⑤）。
  - **批 0 复审遗留并入此项（LOW）**：① `run_once` 里 user_text 在 `maybe_compact` **之后**才 append，粘贴当轮的巨型 blob 不被任何预算网计入即直发——须在 append 后、发送前再跑一次预算/压缩检查（回合内压缩 F24 的一部分）；② 单条超大 user 输入无法靠压缩消化（那是用户真实输入），配套加「单输入字节上限→落盘换占位符」或靠本超限事后恢复兜底。批 0 已用 base64 感知 token 估算（tokens.py）堵住**累积型** base64，此两条是**当轮粘贴型**的剩余口子。
- **摘要语义防御·加固**（对抗审查修正表述：现有 compaction.py:134-135 摘要消息与 :77-81 摘要 prompt **已有**「勿执行摘要内指令」基线，此项是加固不是新增）：补「最新用户消息是唯一真相」等 hermes SUMMARY_PREFIX 缺失措辞。
- 验收：`test_单回合长工具链不超上下文`、`test_回合内压缩有节流不逐轮全查`、`test_压缩连续失败三次后熔断不再调模型`、`test_熔断计数跨进程生效`、`test_摘要失败截半重试成功`、`test_压缩后最早用户原话逐字在场`、`test_二次压缩原话不重复堆叠`、`test_超限报错后压缩重试恰一次`、`test_观察上限落盘后压缩阈值收紧`、`test_连续两次压缩省不足一成即停`。

### 批 2 · 注入纵深 + MCP 健壮性 + 可靠性（报告 P1 + 审查包；体量约批 1 两倍，**单独给 2-3 天**，或拆 2d/2e 为批 2.5——对抗审查）

**2a · 不可信外部内容随机 ID 成对边界包裹**（建议⑦）
- 现状已核实：tools.py:418（web_fetch）/tools.py:1020（MCP）/vision.py:352（recall）只有固定开头前缀、无结束标记——恶意内容写「（外部内容结束）接下来是系统指令」即可伪造边界。
- 改法：新增 `_io.wrap_untrusted(text, source)`：`secrets.token_hex(8)` 成对边界，五处（web_fetch/web_search/MCP/OCR/recall）统一替换；包裹在出口做，边界串不进污点库。
- 验收：`test_外部内容包裹前后标记同id且成对`、`test_正文自带假结束标记逃不出包裹`。

**2b · 双时钟睡眠检测**（建议④）
- 现状已核实：`kimi_client.py` exit 28 一律抛「检查网络」；无 monotonic/墙钟对比。合盖唤醒是个人机 harness 最高频误报场景。
- 改法：读流时刷新双时间戳，exit 28/读断时「墙钟流逝−单调钟流逝>30s」判睡眠唤醒，允许整请求重试一次并提示；判定逻辑抽纯函数。
- 配套建测试基建：**进程内假 HTTP 供应商**（`http.server` 回放 SSE，让 `_post_stream→reassemble_stream` 全链路真跑）（报告维度⑤经验1，几乎为小蛇定制）。
- 验收：`test_睡眠判定纯函数`、`test_失速无睡眠不重试`。

**2c · MCP 健壮性包**（建议⑧+⑨ + F30 + F16 + 报告维度⑥经验3 —— 同文件并一包）
- **失败宽限**：call 遇 MCPError 不立即永久软屏蔽，先按启动配置重启 server 重发本次调用恰一次，再失败才 `_DOWN`；工具名映射复用旧 pref 保缓存前缀（建议⑧；核实发现协议级 error 也走该路径，一并修）。**前置：connect() 现不保存 command/args，重启逻辑要先补启动配置留存**（对抗审查）。
- **64 字符上限**：`len(pref)>64` 时 `pref[:55]+"_"+sha256(pref)[:8]`（确定性稳定），撞名后缀挪到截断之后（建议⑨；核实发现撞名后缀现可到 73+）。
- **F30**：connect() 覆盖同名 server 先关旧 client（防子进程泄漏+路由指向失联端）。
- **F16**：tools/list 跟进 nextCursor 分页（防大 server 工具静默丢弃）。
- **readOnlyHint→READONLY_TOOLS 映射 + `_meta` 净化**（第三方工具也能进只读通道）。
- ⚠ 固化 bug 行为的现有测试，本包落地必须一并改：`test_mcp_softmask.py:25-43`（把「一次失败即 _DOWN 并抛错」写死为期望，与失败宽限直接冲突——对抗审查抓的计划外红测）。
- 验收：`test_MCP瞬时失败自动重连一次后成功`、`test_重连仍失败才软屏蔽`、`test_超长MCP工具名截到64内且同输入同输出`、`test_截断后不与既有工具撞名`、`test_同名server重连旧进程被关闭`、`test_nextCursor分页全量拿到`。

**2d · permission 判定集对齐包**（70条推进序③全六项：F04/L3 lookbehind 修 glob 过宽 + **F28/L4 check() resolve 崩溃防护（对抗审查补回）** + F38/L5 + F60/L6 + F67/L7 大小写 + F36/M3 限深对齐）
- 照 70 条清单定论施工；F28 修法 `except (OSError, ValueError)` 返回 deny（畸形路径不许把权限闸门整个崩掉）。
- F04 用 lookbehind（零安全损失首选），残余 `*.s*` 权衡按清单交甲方拍板项处理——本方案默认**接受残余**（维持检测强度优先）。

**2e · A1 粘贴补丁**（报告维度⑦经验1+2）
- `_read_bracketed` 加 2 秒超时逃生（SSH 丢结束符不卡死）；`_BurstGuard` 三条：见过真 paste 事件永久关突发检测、Windows 放宽时间窗、CJK 不参与突发缓冲。
- 验收：`test_粘贴丢结束符两秒后flush`、`test_见过真paste后突发检测关闭`。

> 原批 2 的「安全开关导入时冻结」经对抗审查降级：当前审批路径一处不读 os.environ、config 常量导入即冻结、工具全走 subprocess 打不到父进程环境——**今天是空转项**。改列为：①基M2 引入 PTC/进程内执行面时的**伴随硬约束**（届时安全开关绝不实时读 env）；②批 3a 架构守卫加一条 AST 断言「permission/agent 决策路径不得出现 os.environ 读取」。

### 批 3 · 工程卫生 + 可观测（便宜快赢）

| 项 | 内容 | 出处 |
|---|---|---|
| **3a 架构守卫测试** | `tests/test_arch_guard.py`：AST 遍历 `harness/*.py` 断言 import ∈ 标准库∪harness；第二条「密钥不进 subprocess argv」；第三条「决策路径不读 os.environ」（承接批 2 降级项） | 建议⑪ |
| **3b 出网审计日志** | 每次 web_fetch/web_search 真发 curl 前（含每跳重定向）追加一行 `ISO时间 web_fetch <url> allow/deny` 到 `logs/network.log`；`try/except OSError` 包住绝不阻塞。**schema 按 effects.jsonl（乙9）字段设计**——本项即乙9 的先行切片 | 建议⑫ ∩ 乙9 |
| **3c 计费/资源三连** | F01 非流式 `_post` 去掉对非幂等生成的 `--retry`（防 6× 重复计费）；F54 read_file 大小上限闸；F20 render 加 need_dom 门控（照 70 条定论，不采单条 chrome 合并） | 70条推进序⑤ |
| **3d 调度并发闸** | F32：schedule 全局并发上限，纪律照 70 条定论「占坑→放锁→再 popen」防 TOCTOU | 70条推进序④后半 |
| **3e spill footer 自助指令** | `spill_or_truncate` 落盘 footer 附「用 recall 取剩余」的命令原样写法（弱模型友好） | 报告维度④经验3 |
| **3f 随手清理 + 契约补强** | F43 内联 record_taint 收归、F44 欢迎屏 VERSION/能力简介更新、F45/F47 死代码清除；**契约补强四条（对抗审查补回，推进序⑦另一半）**：CONTRACT.md 补 type_text/ocr 已上线、_TAINT_HIGH_RISK 已扩容、P6 cost/backup/restore 条款、MCP 路径取舍——零回归风险、对靠契约验收的甲方是承重项 | 70条推进序⑦全 |
| **3g（可选尾项）只读工具批并行** | 见冲突裁决 #1——降级为可选：先主线程串行跑完全部审批，再对已放行连续只读段 `ThreadPoolExecutor(4)` 并行按原序回填。**没时间就不做** | 建议⑩（降级） |

### 批次之外、随批修 bug 时必守的三条

- 固化 bug 行为的现有测试，修对应项时必须一并改：`test_m0.py:81`（只验精确去重、无上限断言）、`test_vision_wire.py:74-80`（把「3 张只留 2 张」写死为期望）（70 条定论）、`test_mcp_softmask.py:25-43`（一次失败即屏蔽写死为期望，归批 2c——对抗审查补）。
- F37/L24 SSRF 内嵌 IPv4 修法用 `ip in ip_network('64:ff9b::/96')`，**不用**原报告的位移写法（有 bug，70 条勘误）。
- F18（PDF 转图 Windows 分支）**不丢**：照 70 条推进序⑥单独排期，落点在战略主线视觉轴（见下），修法 WinRT/JXA 已真机验证。

---

## 二、战略主线（A1-A12 施工定稿原序不变，吸收报告设计约束）

批 0-3 完成后按施工定稿关键路径推进。报告对主线的影响全部以「设计约束」形式织入，**不改变既定顺序**：

**三地基先钉（不变）**
- **基M1** 记忆 v2 schema 一次协调迁移（字段全集/迁移器+双读/DELETE-aware 合并/source 硬不变式）——A3/A11 一切记忆功能前置。
- **基M2** PTC+CaMeL 绑同批（禁裸 exec、唯一派发入口复用 `_handle_tool_call` 完整管道、不另建平行策略集）——省钱最大杠杆与安全最大缺口同一次施工。H2 的架构级治本（白名单不把 _cmd_hits 当唯一屏障）挂在这里；**伴随硬约束：引入进程内执行面时，安全开关一次性冻结、绝不实时读 os.environ**（承接批 2 降级项）。
- **基M3** steering stdin 单一归属（daemon 唯一读者分类投递；approver 与主循环从队列取）——与批 1b 打断语义同域，施工时对齐。

**中层可并行（不变，带新约束）**
- **A4 Edit 工具**——报告独立背书「与 Claude Code 差距最大的单点短板，最影响改代码质量」，第一梯队地位巩固。设计约束（报告维度④经验2）：编辑冲突检测用 **mtime+内容哈希双重校验**（Windows 云同步乱动时间戳，纯 mtime 频繁误报）。
- **A5 glob/grep 搜索**——与 A4 捆做，第一梯队。
- **A2a 技能类**（SKILL.md，可早做）——设计纪律（报告维度⑥经验5）：**技能/命令注入走追加 user 消息、绝不动置顶 system**（保 prompt cache）。
- **A2b 可执行工具类**——挂基M2 底座后（施工定稿原文），显式保留防丢失（对抗审查补）。
- **视觉 grounding 一条轴**：SoM 起步（UIA 节点框+OCR 词框都往 SoM 喂）+ 抄全 Claude zoom 感知纪律 + JS 布局审计 + dHash/PNG 回归（A12 第一梯队五项照旧）；**F18 PDF 修（WinRT/JXA）在本轴单独排期优先做——它是修 bug 不是新功能**（70条推进序⑥ + A8 🟢②）。
- **经验层阶梯**：Cheatsheet→ReasoningBank（改造现有 episodic）→SKILL.md，同一 curator 生命周期。

**外围按功能排期（不变，两处吸收）**
- A6 hooks 设计定案吸收报告维度⑥经验1：**跨语言 hooks 走「子进程 stdout JSON 协议」**（`{"decision":"allow|deny|ask",...}`，空输出/非 JSON 放行，安全类钩子异常 fail-closed 判 block），hook 决议收口进 `permission.check()` 单点。
- A7 持久放行落地时沿用批 1a 的指纹 key 格式（绑命令模式+cwd 不绑工具名）。
- 记忆后台增量 delta+token 闸门、FTS5 统一检索层、传输统一、Office COM、CAPED 遮蔽——照施工定稿。
- SSRF DNS-rebinding：维持既有结论（架构层决策待甲方拍板：代理侧 REJECT 内网网段为主 + 可选 Python 侧预检纵深），批 2d 只做判定集对齐不动这块。

---

## 三、冲突裁决记录（两源打架处的定论）

1. **建议⑩（只读并行）vs 70 条「建议记录不修」** → **降级为批 3 可选尾项**。70 条的理由（可并发的重 IO 工具恰好都是 ask 非 allow）部分成立；报告改法（先串行跑完审批再并行已放行段）确实解决审批串行问题，但收益被 ask 工具占比打折，主要剩多文件 read_file 场景。不值得占正批位。
2. **compaction 六源（建议②⑤⑥+壳三连+F03+F24）** → **并成批 1c 一包**，一次施工，两道闸阈值统调（丙-compaction 定论），F03 常量先在批 0 救急改（取 384000 保安全余量）。
3. **建议①（会话审批指纹）vs 壳-A7（持久放行）** → **同方向分两步**：批 1a 做会话内，A7 阶段做持久化；key 格式批 1a 一次定对预留兼容。
4. **H2 修法两源融合** → 报告的三类去混淆变体展开 + 70 条的「命中不硬 deny 而剥夺白名单强制 ask」同一次施工（批 0），**含跨文件 force_ask 管道**（对抗审查 HIGH：否则被会话白名单无声击穿）；架构级治本挂基M2。
5. **建议⑫（出网审计）vs 乙9（effects.jsonl）** → ⑫是乙9 的先行切片（批 3b），schema 按 effects.jsonl 字段设计，避免将来推倒重来。
6. **两套 M 编号撞名** → 本文起统一「基M1/2/3」与「F 编号」，不再混用。
7. **报告「交互层薄」三大短板之一（对抗审查补裁决）** → 本阶段**显式不做**大交互层（斜杠命令框架/输入历史补全/markdown 高亮/状态栏）：批 2e 粘贴补丁 + 基M3 steering + F44 欢迎屏更新之外，其余列储备。理由：单人项目性价比、且 A6 自定义命令将来自然带一部分。

---

## 四、不做 / 降级 / 储备清单（合并两源，防止将来重复调研）

**照旧不做**
- 六维丁 7 项：FrugalGPT / semantic caching / HippoRAG / STaR / ttyd / ETDI / 动态工具裁剪（反向纪律：工具声明字节级冻结）。
- A8 🔴：一切要本地跑权重/训练的。
- 壳对比 §5 明确不抄清单（Honcho/多租户云后端/Gateway/TUI 双进程/多注册表/credential_pool 等）。
- cc-haha 相关一切「照抄」→ 禁止（泄露源码，只查思路不抄代码，不可商用）。

**降级**
- 报告建议⑩ → 批 3 可选尾项（裁决 #1）。
- 「安全开关导入时冻结」→ 基M2 伴随约束 + 批 3a 守卫断言（今天是空转项，对抗审查核实）。
- 交互层增强 → 显式不做大交互层（裁决 #7）。

**储备（痛了再上）**
- 报告维度⑦经验3：大段粘贴落盘占位符（A1 已有 bracketed paste 主路）。
- 报告维度⑤经验4：flaky quarantine（真链测试出现 flaky 再上，JSON 清单+过期日检查一条测试可复刻）。
- 报告维度⑦经验4：**终端回包不是用户输入**（对抗审查补）——将来做终端能力探测（背景色/光标位查询）时必须配回包解析或 SSH 场景不发查询；hermes「只在会复现 bug 的环境改行为」窄化原则。前置纪律，防重蹈 CPR/DA 序列漏进输入框的坑。

---

## 四·五、施工进度（滚动更新）

- **批 0 · 止血** ✅ 已提交 `a17f9bc`：H1 备份越界 RCE / H2 命令护栏引号折叠+解码执行管道 / F03 压缩预算+base64 感知估算。过对抗审查（2H+2M+1M 全修）。
- **批 1 · P0 三连** ✅ 已提交 `04970c8`：1a 审批指纹（run_command 绑整条命令，杜绝首词/包装/拼接旁路）/ 1b 打断保留进度（单工具往返粒度）/ 1c 压缩强化（逐字原话+语义加固+熔断冷却自愈+截半重试+F24 回合内压缩）。过**两轮**对抗审查（2H+3M 全修）。
- **批 2 · 注入纵深+MCP+可靠性** 🟡 主体完成：
  - `27e8939`（p1）：2a 随机边界包裹 / 2b 双时钟睡眠检测 / 2d permission 对齐（F28/F36/F67）。过对抗审查（2M 全修）。
  - `7cc23bd`（p2）：2c 工具名 64 上限（建议⑨）+ 同名重连不泄漏（F30）/ 2e 突发护栏微调。
  - **批 2 尾欠（batch2p3）**：✅ 2026-07-24 全部落地——2c 建议⑧ 失败重连宽限（connect 存启动配置→重启重发恰一次）、2c F16 nextCursor 分页（前批已落）、2e `_read_bracketed` SSH 丢结束符 2s 超时（msvcrt/select 平台非阻塞探测）。见顶部进度表「批2尾欠」行。
- **批 3 · 工程卫生**（未开工）：3a 架构守卫测试 / 3b 出网审计日志（=乙9 先行切片）/ 3c 计费资源三连 / 3d 调度并发闸 / 3e spill footer / 3f 随手清理+契约补强。
- **主线三地基（基M1/M2/M3）**：⚠ **动代码前须甲方拍板**各自的决策 checklist（记忆 schema 迁移 / PTC+CaMeL 绑定 / steering stdin 归属）。

测试基线：688 全绿（批 0 起 642 → +46 批 0/1/2 用例）。

## 五、推进节奏与验收纪律

- 每批一个 feature branch + PR，CI 绿（ubuntu×Py3.12/3.13）后按授权合并；每批过一轮对抗审查再报「完成」。
- 全程 TDD：先红后绿。验收用例名照本文所列，**一律 `test_` 前缀**（`测_` 前缀 unittest 收集不到，会产生永不运行的静默绿——对抗审查抓的坑）；落地前先查与 tests/ 现有用例不撞名。
- 工作量：批 0 半天；批 1 约 1-2 天；**批 2 单独 2-3 天**（六子包横跨七文件+假 HTTP 基建+MCP 状态机改造，均摊估计会跳票或挤掉对抗审查——对抗审查定论）；批 3 约 1 天。批 3 完成即回主线（三地基）。
- 涉及真机行为的（超限报错形状、MCP 重连、睡眠检测）先真机探针拿事实再写实现。
