# 小蛇 Harness 交接入口（2026-08-04）

这是可直接接力的完整工作副本。包含当前代码、完整 Git 历史、实施方案、验收资料，以及未提交但必须保留的交接工作树改动。

## 本机同步状态（2026-08-04）

- 已从 `/Users/example/Desktop/小蛇交接` 以校验和方式同步到当前目录；不使用删除同步。被覆盖的 10 个文件已保存在 `/Users/example/Desktop/小蛇-同步前备份-20260804.NjP8Tf`，可按需恢复。
- `.env`、`.state/`、`logs/`、`memory.json` 与 `memory.json.lock` 被明确排除；其中本机的 `.env`、`memory.json`、`memory.json.lock` 已校验未变。
- 当前本机 Git 分支是 `main`；交接来源记录的原始工作分支是 `codex/plan05-effects-plan06`。两者都必须保留继承改动，禁止以 reset、clean 或 checkout 覆盖工作树。
- 在本机 Python 3.13.12 上，Plan05/Plan06 的 67 项聚焦回归与 `compileall harness` 已通过；系统默认 Python 3.9 不支持项目所需的 `datetime.UTC` 和新版 f-string，不能用于验收。
- Plan06 仍停在 Task1：最新独立审查的两个 P1 尚未关闭，尤其是 `TaskQueue.enqueue` 必须先拒绝终态/归档任务、再处理同一 `trigger_key` 的幂等返回。因此**不得开始 Task2**。

下一步请从 [本机同步验收与续作入口](docs/handoff/接力说明-2026-08-04-本机同步与续作.md) 开始，再处理 [Plan06 最新接力说明](docs/handoff/接力说明-2026-08-04-Plan05-Plan06.md)。

交接来源包**不包含** `.env`、`.state/`、运行日志、缓存或任何本机密钥。当前目录保留本机原有的运行配置，但新设备仍必须依据 `.env.example` 单独配置密钥；不得复制旧设备密钥。

## 先读

1. [最新接力说明](docs/handoff/接力说明-2026-08-04-Plan05-Plan06.md)
2. [总体验优化与转型方案](小蛇完善方案/小蛇Agent工作台转型完整方案.md)
3. [实施计划目录](小蛇完善方案/implementation-plans/)
4. [Plan06 执行账本](.superpowers/sdd/06-后台任务与任务收件箱/progress.md)

## 当前状态

- 交接来源的原始分支为 `codex/plan05-effects-plan06`；当前本机目录保留在 `main`。不要 reset、clean、checkout 覆盖或删除任一工作树中的继承改动。
- Plan05「工作树与任务级恢复」的代码、聚焦测试和独立审查已通过；恢复只修改 manifest 文件，外部不可逆效果只会展示并要求明确确认，不会重放或宣称可撤销。
- Windows 原生验证仍待环境能力：当前账户创建测试符号链接时得到 `WinError 1314`。这是账户权限门槛，不是代码失败，也绝不能记作已通过。
- Plan06「后台任务与任务收件箱」仅完成 Task1（持久化 TaskQueue，提交 `483d452`），但**尚未验收通过**：终态任务能借已有 trigger 重返队列，且该提交纳入了交接遗留的 TaskModel/TaskStore 前置代码。先按最新接力说明修复/确认，不能进入 Task2。

## 启动与最小验证

```powershell
py -3 run.py serve --no-browser
```

需要 Task 工作台时：

```powershell
$env:XIAOSHE_TASKING_V2 = 'on'
py -3 run.py serve --no-browser
```

Windows 原生验证（先启用开发者模式或为账户授予创建符号链接权限）：

```powershell
powershell -ExecutionPolicy Bypass -File .\scripts\verify_windows.ps1
```
