# 小蛇流线背景（已封存）

状态：实验保留，产品界面默认停用。

## 封存原因

- 空会话阶段持续重绘会带来不必要的 CPU/GPU 与内存压力。
- 当前重点转向小蛇品牌化的工作台、对话与设置体验。
- 视觉方案本身保留，后续可在独立预览或性能预算明确后继续迭代。

## 最后确认参数

- 目标帧率：24 FPS
- 周期：11 秒
- 采样点：104
- 线束数量：17
- 中心纵坐标：380
- 横向跨度：1080
- 雾化层：1 / 20 / 55

## 代码位置

封存时的几何、绘制与挂载实现仍保留在 `client.js` 的
`xiaosheRibbon*` 函数组中，但产品入口由
`XIAOSHE_RIBBON_BACKGROUND_ENABLED = false` 明确关闭。

恢复实验时只能在独立预览环境启用；不要直接恢复到默认空会话。
