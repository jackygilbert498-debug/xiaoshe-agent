# XS 2026-09-20 接收与继续开发基线

## 当前结论

另一台 Windows 设备的开发快照已合入 `/Users/zfy/Desktop/XS`，保留这台 Mac 独有的修复。当前基线是**本工作区实际文件**，不是未更新的 Git HEAD，也不是原封不动的 Windows 快照。已完成本机构建和下述回归，具备继续开发的条件；未把源码同步称为日常实例已更新或生产发布已完成。

来源 `/Users/zfy/Desktop/XS交接` 保持原样。其 `handoff-manifest.json` 已在复制前独立验证：8,031 个文件、66,280,283 字节，来源摘要 `964d1d82301ddd366f81851eaf7124061b396b60888718cedc8f97768a47c8da`。两边 Git 来源均为 `eefbaae3701d78f6d3cca4eec283f85e32655767`，但都有提交以外的开发进度，不能用这个 HEAD 代替当前源码。

合并前，两边共有 7,926 个逐字节相同的文件、70 个内容不同的文件，交接包新增 35 个文件；本机选定源码中另有 13 个来源包没有的文件。没有按时间戳覆盖，也没有删除本机独有文件、清理工作树、提交或推送。

## 接入的进度

- **Loop + Graph**：复杂任务可选依赖图，沿用既有 Goal 续轮；不引入第二套调度器。节点、尝试、证据与反馈写入 Session Log；桌面任务页和终端 `:tasks` 读取同一事实源。
- **失败恢复**：历史失败次数、相同只读结果不再封工具或能力族。真实权限、任务路径限制仍有效；结果未知的外部发送/提交保留精确重复保护，迟到的旧查询不能解除新的未知操作。
- **工作台与界面**：待回答/审批和异常优先；旧任务图折叠，长任务图保留当前重点与展开入口；验收条件、反馈更易读。接入外观设置、字号/图片取色、材料目录和启动反馈等更新。
- **项目知识与记忆**：新增可关闭的源码知识插件；按文件及显式依赖哈希判断摘要是否过期，不代替源码事实、用户记忆或会话记录。接入记忆上下文预算。
- **安装组成**：桌面/终端、产品包、macOS/Windows 安装与启动清单包含新增知识模块；修复发布包依赖使用源码相对路径的问题。

Graph 首版最多 64 个节点，同时一个活动图节点，节点内部可并行工具。`reviewer-assessment` 是关联真实结果的模型评估，不是独立审计或任意业务正确性的保证。Graph 的记账问题不能阻断普通工具。

## 特别保留的本机修复

1. **不适用的检查不阻塞收尾**：脚本、plist、文档等场景中，经过证据确认的 `not-applicable` 不记为通过，也不误记成未完成；类型、持久化、回放、回执和提示链路一起保留。
2. **视觉恢复及地址校验**：保留同一图片哈希下的工具内有界超时恢复、进程清理与取消处理；已有图片不会被误说成用户缺图。本地路径不能被浏览器拼成假的 HTTPS 地址。原先按超时次数/冷却时长封调用的策略已由新版建议式恢复替代，没有重新带回旧封禁。
3. **执行边界**：保留 shell 重定向与代码文本的区分、授权项目检查的正常执行、受保护文件解析和新预览文件的准备逻辑；仍不允许越过用户明确的禁止和路径限制。旧的按策略拒绝次数强制取消逻辑已移除，测试改为检查“拒绝越界，但保留有效恢复”。
4. **模型与会话**：模型目录不依赖历史投影恢复而永久卡在加载；保留“移入项目”入口与对应测试；保留 V0/V1/V2 历史会话到 V3 的校验及引用坐标迁移。
5. **macOS 启动**：保留依赖平台/执行权限检测、现有原生模块及本机启动文件，不导入 Windows 的 node_modules 或文件权限。

两类合并回归已实际发现并修复：本机收尾辅助模块也须改为已声明的包依赖导入；菜单渲染夹具须保留项目列表参数。此外，交接工具测试使用规范化后的 macOS 临时目录，未放宽生产代码拒绝不安全链接的规则。

## 本次验证

环境：macOS，Node `v24.19.0`，pnpm `11.7.0`。根工作区使用锁文件离线安装链接、关闭安装脚本；没有重新下载或覆盖 Windows 依赖目录，没有改个人凭据。

根包与有对应入口的 XS 工作区包完成构建/类型检查；DSH host/client 完成构建与 TypeScript 项目检查；macOS 依赖诊断通过。

以下统计只计最终运行，不叠加修复前失败或重复重跑：

| 范围 | 通过 | 跳过 |
| --- | ---: | ---: |
| Agent 标准单元入口，含经验、验证策略、完成回执三个包 | 618 | 3 |
| 标准集成入口的测试部分，含 Graph 与安装后导入 | 572 | 2 |
| 桌面壳全包测试，含真实隐藏 Electron 渲染 | 168 | 0 |
| memory / project-knowledge / runtime-contract / runtime-dsh-provider / terminal-client / product-bundle | 179 | 1 |
| DSH Session、历史格式兼容与通用设置，21 个测试文件 | 574 | 0 |
| 源码交接、桌面启动反馈、macOS 依赖与版本检查 | 33 | 0 |
| **合计** | **2,144** | **6** |

最终上述范围 0 失败。6 项跳过均为 Windows 专用用例，不算通过；不是整个仓库所有测试的声明。Graph 43 项包含在集成范围，不重复加总。

隐藏 Electron 使用合成任务，检查 248/400 像素栏宽、明暗主题、长文本、键盘展开、当前问题优先与历史图折叠，并目视检查本地截图。ModLens 注册/桥接集成使用隔离的模拟引擎，不等于本轮完成真实付费模型读图。没有操作用户业务数据或替用户处理实际任务。

详细日志、截图、差异与备份在 `output/stabilization/receive-20260920-kDptsI/`。其中 `merge-manifest.json` 是合并前清单；`resolutions.json` 是初始合并决策；最终逐文件状态以 `final-audit.json` 为准。

## 日常运行与尚未验收的边界

本次没有启动/重启日常后台或刷新桌面，没有改 Profile、API Key、浏览器登录态、系统服务或 shell 命令注册。只读版本检查时，默认 `127.0.0.1:3080` 后台不可达；前端构建摘要与当前源码一致，已加载窗口版本未知。不能据此宣称用户日常窗口已经加载新功能。

来源设备的真实 DeepSeek Graph 请求曾在工具调用前以 `TRANSPORT` 结束；这是来源记录，本轮没有重复该付费调用，也不推测根因。真实模型驱动、多步骤业务任务的最终完成质量仍待专项验收。Windows 实机、签名公证、发布及跨设备冷安装也未在本轮完成。

## 接手后的优先顺序

1. **激活并核对版本**：在确认用户日常任务与草稿状态后，从当前源码启动，让既有启动器更新必要的产品插件；核对源码、构建产物、后台和窗口加载摘要。不要回退旧打包应用。
2. **补真实模型闭环**：使用合成资料及隔离会话，覆盖简单文件任务、多步骤依赖图、同工具失败后修正、等待恢复、独立回读、桌面/终端一致性。明确区分工具成功、模型评估与任务实际完成。
3. **把真实失败固化为回归**：优先改小蛇现有驾驭工程，不换底座；继续维护“不适用门禁不拦收尾”“普通失败不封工具”和“不重复未知外部副作用”三条边界，再评估发布准备。

可重跑入口（需使用受支持的 Node 24 PATH）：

```sh
pnpm -r --if-present run build
npm run build
npm run typecheck
pnpm -r --if-present run typecheck
npm run test:agent
npm run test:agent:integration
npm run test:installed-imports
npm run test:graph
pnpm --filter @xiaoshe/native-shell-legacy-adapted test
node --test scripts/handoff-source.test.mjs
```

## 可恢复性与工作纪律

修改前原件位于备份目录 `before/`；Git 索引和完整工作树状态单独留档，未 reset、clean 或暂存。原接收记录另存 `_接收记录.before.md`，下方旧记录仅供历史参考。

恢复时先对照最终清单核实目标自本轮后没有新改动，再逐文件恢复对应原件；新增文件须单独审阅处理。不要整目录覆盖、删除源码根目录或直接恢复旧 Git 索引。备份不含个人会话/密钥，因为本次没有修改它们；构建输出与依赖为可重建内容。
