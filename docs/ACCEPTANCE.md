# 验收清单

## 自动化

- [x] TypeScript 严格类型检查通过。
- [x] Node/前端测试覆盖桥接成功、异常 JSON、崩溃、超时、中止、审批组合、动态工具注册、本地状态路由、产品视觉契约、客户端注册、示例预填、左右栏收放偏好、脱敏证据卡、交接清单、Windows 隔离验收和 launchd 最终进程监督（56/56；另有 1 项 macOS 真执行测试在 Windows 跳过）。
- [x] Python 单元测试覆盖观察、缩放映射、陈旧视口拒绝、动作后验证、私有文件权限、主屏元素过滤、AX/UIA 目标局部视觉复核、窗口目标约束和 Windows 现代 PowerShell UIA 选择（15/15）。
- [x] 构建产物只依赖 Node 内置模块，不隐式解析 DSH 私有包。
- [x] Bundle 能安装进实际 `web` Profile，`--dump-config` 显示独立层且保留 ModLens。
- [x] DSH Web 真实启动，HTTP 200，日志中无插件加载错误和孤儿桥接进程。

## G3 统一产品体验

- [x] 浏览器标题、侧栏品牌与空态主视觉显示“小蛇”，底层仍为唯一的 DSH 会话与 Agent Runtime。
- [x] 真实 Microsoft Edge 已验收深色/浅色宽屏、左右栏同时收起、窄屏抽屉关闭/打开/再关闭及既有会话活动态：左栏 280→56px、右栏 320→52px、顶部抬头同步定位，所有场景横向溢出 0、页面错误 0。
- [x] 首屏只保留一句产品承诺，以及“看得见桌面／真能动手做／关键操作可控”三项静态特点；原 01–03 通用示例任务及其预填行为已移除。三项特点采用无背景、无圆角、细线分隔的能力标线，不再呈现为笨重胶囊或伪任务按钮；真实宽屏测得标题→说明 12px、说明→特点 15px、横向溢出 0、页面错误 0。
- [x] 正式蛇标、侧栏中英文品牌和首屏品牌均复用同一方瞳母本；品牌英文声明为不可翻译，Edge 不再把 `HARNESS · ATELIER` 误译为“安全带 · 工坊”。
- [x] 深色主题真实键盘导航已逐项验收“标准模式”“工作区写入”“设置”：默认亮蓝框已替换为小蛇墨玉绿焦点环，鼠标点击不残留焦点装饰，Tab 可见性保留。
- [x] 设置页显示 Bridge、ModLens、屏幕权限和动作开关；动作开关可持久化并实时改变模型工具表。
- [x] Terminal 宿主的未授权探针真实返回 `SCREEN_CAPTURE_FAILED`；正式 `ss`/launchd 宿主的成功探针返回真实 PNG、AX 元素和一次性预览 URL，证明 TCC 状态按宿主区分。
- [x] 预览接口具有同源/回环限制、一次性随机 token、私有文件复核、大小上限和 `no-store`。
- [x] 动作结果元数据可随会话回放；卡片显示操作前后视口、变化和元素增删，输入原文不会进入工具卡。
- [x] `启动小蛇.command` 复用健康实例、拒绝误杀端口占用者，并通过 `launchd` 在启动终端退出后持续运行。
- [x] `停止小蛇.command` 只移除固定的小蛇用户服务；launchd PID 与 3080 监听 PID 相同，停止、端口释放、再次冷启动均已实测。
- [x] 新终端 `ss` 已指向 DSH 小蛇统一界面入口。
- [x] 授权后的真实截图、私有预览令牌和 `screen_observe → modlens_read_image` 现场链路已通过。
- [x] 一次批准后的真实动作证据卡。隔离验收窗口的 AXButton 经 `allowed-once` 后被精确点击，结果卡经真实会话浏览器回放。
- [x] 视觉 V3 的尺寸、收放、浅色恢复、活动态计数与截图证据已记录在 `docs/evidence/2026-08-21-ui-v3-validation.md`。
- [ ] `s` 终端入口迁移到共享 DSH Runtime。当前保留旧 CLI 兼容面，归入 G4。

## macOS 真实闭环

- [x] `screen_observe` 产生真实主屏 PNG、元素表、逻辑/像素尺寸和视口版本。
- [x] ModLens 能读取该 PNG 并返回与画面一致的结构化证据。
- [x] AX 通道在真机读取到元素；ModLens 独立读取本地 PNG 并返回正确 OCR/布局/语义证据。
- [x] 对无副作用测试目标执行一次批准后的点击，动作前目标与局部像素复核通过，动作后视口从 `v4` 变为 `v5`，验收窗口的 4 个 AX 元素消失。
- [ ] 使用旧视口再次点击被拒绝，且没有发送鼠标事件。
- [ ] 输入和按键只有在审批允许后执行；拒绝时目标应用无变化。
- [x] 关闭 `XIAOSHE_DESKTOP_ACTIONS` 后三个动作工具不出现在模型工具表。

当前边界：正式 `ss`/launchd 宿主已经具备屏幕权限；Terminal 单独诊断仍可能因宿主 TCC 不同而失败。真实 AX 点击已闭环到“元素命中 → `approval/asked` → `allowed-once` → 执行 → 动作后观察 → 浏览器证据卡回放”。使用旧视口的第二次真机拒绝、真实输入/按键审批和多显示器动作仍未验收。

## Windows 真实闭环

- [x] 实际 `web` Profile 同时加载 ModLens 与 XS；浏览器设置页显示 Bridge available、platform `win32`、动作开关持久化，并生成真实私有预览。
- [x] 200% DPI 下真实截图为 3120×2080；UIA 与截图统一使用物理坐标，隔离窗口元素稳定解析。
- [x] 隔离 WinForms 目标完成 UIA 精确点击、13 字符中文输入和 Enter，动作后截图/UIA 状态均变化。
- [x] 复用点击前旧视口发送 `{ESC}` 返回 `stale`，没有发送输入事件。
- [x] 关闭动作能力后，Bridge 执行层返回 `ACTIONS_DISABLED`。
- [x] 顶层窗口枚举只返回唯一标题和临时 ID；重复标题排除。隔离窗口通过完全一致标题完成精确聚焦，聚焦工具属于动作审批族。
- [ ] Windows 真实模型会话的 `approval/asked → allowed-once` 浏览器回放。Provider 动作现场和审批组合自动化均通过，但当前 Profile 没有可用模型会话，未拼成伪现场证据。
- [ ] 125% 与 150% DPI。当前设备为 200%，本轮没有改变用户显示设置。

现场证据：`docs/evidence/2026-08-22-windows-screen-validation.md`。

## 尚需外部设备

- [ ] Windows 11 的 125%/150% DPI 复现；UIA 截图、点击、输入和当前 200% DPI 已通过。
- [ ] 多显示器坐标与主副屏选择。
- [ ] macOS 未授权、授权撤销和系统升级后的 TCC 引导复验。

未授权路径已于 2026-08-20 实测，错误指引正确；“授权撤销”和“系统升级后”仍需外部复验，因此整项保持未完成。
