# 小蛇开发者发行版只提供公开配置，不包含任何密钥。
# 定时提醒与时间上下文由小蛇 bundle 提供；这里不重复插入插件。
# 用户已有的 schedule/time-context/ui-schedule 覆盖继续优先生效。
- id: modlens
  config:
    upstream: deepseek-official
    # 小蛇会在安装期对锁定的 ModLens 运行时补上该配置入口；视觉服务
    # 整次读取（含后端重试）最多 60 秒，超时清理进程并返回明确失败。
    timeoutMs: 60000
