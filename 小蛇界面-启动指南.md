# 小蛇界面 · 启动指南

## 方式一：Web 端一键启动（推荐，零依赖）

双击运行：

- **`启动小蛇界面.bat`** — 正式模式（使用 `.env` 选择的活动提供商）
- **`启动小蛇界面-演示模式.bat`** — 演示模式（假模型，不烧 API，无需 .env 和代理）

启动后自动打开浏览器 `http://127.0.0.1:7788/?token=…`

---

## 方式二：Tauri 真桌面端（需 Rust 工具链）

### 1. 安装 Rust

```powershell
# 用 winget 安装（推荐）
winget install Rustlang.Rustup

# 或官网下载：https://rustup.rs/
```

安装后**重启终端**，验证：
```powershell
cargo --version
```

### 2. 构建桌面端

```powershell
cd tauri
cargo tauri dev          # 开发模式（热重载）
cargo tauri build        # 生产构建（生成安装包）
```

构建产物：
- 开发模式：直接弹出桌面窗口
- 生产构建：`tauri/target/release/` 下的可执行文件 / 安装包

### 3. 桌面端特性

- 独立窗口（非浏览器）
- 系统托盘图标
- 原生菜单/快捷键
- 离线可用（连接本地 serve 服务）

---

## 常见问题

### Q: 启动后浏览器打不开？
A: 检查 `python run.py serve` 是否启动成功，手动打开 `http://127.0.0.1:7788`

### Q: 提示 API Key 未配置？
A: 检查 `.env` 中对应的 `KIMI_API_KEY` 或 `DEEPSEEK_API_KEY`。两者都已配置时会同时显示在模型菜单；菜单底部的“＋ 添加模型”可新增本地模型资料。密钥不会回显，保存也不会偷偷测试连接；测试连接需要你单独确认，可能产生少量调用或计费。`MODEL_PROVIDER` 只决定新会话默认模型。

### Q: 提示「连接失败 exit 35」？
A: 仅启动代理不够：当前提供商为 DeepSeek 时，在 `.env` 设置 `DEEPSEEK_PROXY=http://127.0.0.1:7897`；使用 Kimi 时设置 `KIMI_PROXY=http://127.0.0.1:7897`，并确保对应地址的代理可用。

### Q: Tauri 构建失败？
A: 确认 Rust 已安装（`cargo --version`），或先用 Web 端（.bat）

---

## 界面功能速览

| 功能 | 位置 |
|---|---|
| 消息流（user/assistant/tool） | 中间主区域 |
| 工具调用卡片（可展开） | 消息流内 |
| 审批弹窗（y/n/a/p） | 底部/模态 |
| 状态面板（todos/notes/jobs/子agent） | 右侧栏 |
| 屏幕观测（SoM 编号截图） | 右上角「屏幕观测」按钮 |
| 命令面板（⌘K / Ctrl K） | 任意处按快捷键 |
| 会话列表 | 左侧栏 |

---

**推荐**：先用 `启动小蛇界面-演示模式.bat` 体验（无需配置），再决定是否需要 Tauri 桌面端。
